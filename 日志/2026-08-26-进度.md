---
name: 2026-08-26-进度
description: 模型打子站中转切换 + deepseek-v4-flash GA 正式版全链路调研
metadata:
  node_type: memory
  type: log
  modified: 2026-08-26
---

# 2026-08-26 进度

## 🔄 火山 Agent Plan 复盘 + deepseek-v4-flash 正式版(GA) vs 普通版全对比

### 背景
用户之前已切到火山 Agent Plan（`ark-code-latest` → `deepseek-v4-flash-ga-260731`），但体验差：问几句话烧掉 122/2000 套餐额度。决定换回火山普通版，但最终选择切换到另一个 DeepSeek 中转站。

### 已完成
- **GA vs 普通版全面调研完成**（子 agent 46 次网页抓取，信源：DeepSeek official pricing + changelog + thinking mode + 火山方舟文档）
- **核心结论**：`-ga-260731` 中 `-ga-` = General Availability（正式发布版），`260731` = 2026-07-31（DeepSeek 官方把 V4-Flash 从 Preview 升级为正式版的日期）。两者模型结构/参数量**完全相同**，GA 仅重新后训练，Agent/编码能力更强（Terminal Bench 82.7、DSBench-Hard 59.6）。**官方口径同价**，不区分预览/GA 定价。
- **关键发现**：默认开 thinking（返回带 `thinking` 字段），思考量是烧额度大头之一

### 后续决策
- 用户决定切换回非 GA 版本，但保留在火山平台 → 最终改为切换到**模型打子站**（第三方中转）

## 🆕 模型打子站（moxingdazi.com）中转切换

### 探索过程
- 用户提供 key `sk-silievo-dd9605ab2dab1606cd90a804033fe76d` 和模型 `deepseek-v4-flash`，但不知道 API 地址
- 主域 `www.moxingdazi.com` 是 SPA 前端（New API 框架），API 端点全返回 405
- 探测二级域名发现 `api.moxingdazi.com` 才是真实入口
- **实测验证（全部通过）**：
  - `GET /v1/models` → 200，返回 7 个模型（`aliyun/deepseek-v4-flash`、`aliyun/deepseek-v4-flash-0731`、`aliyun/deepseek-v4-pro-0813`、`deepseek/deepseek-v4-flash`、`deepseek/deepseek-v4-pro` 等），`owned_by: "silievo"` 确认 key 绑定
  - `POST /v1/chat/completions`（OpenAI 格式）→ 200
  - `POST /v1/messages`（Anthropic 格式）→ 200，`x-api-key` 和 `Bearer` 鉴权都通
  - `POST /anthropic/v1/messages` → 404，不支持 `/anthropic` 前缀
- 基于 New API 框架，网友自建，存在套壳风险

### 切换配置
- 已写入 `switch-api.js` 新 preset `moxingdazi`
- 已执行切换：Base URL = `https://api.moxingdazi.com`，模型 = `deepseek-v4-flash`，鉴权用 `ANTHROPIC_AUTH_TOKEN`（Bearer）
- 旧 API_KEY 已清除，新旧不冲突
- 沙盒网络白名单已加入 `api.moxingdazi.com`

### 用户需执行
- `/exit` 完全退出 → 重新打开 Claude Code → `/status` 确认 Base URL 正确

### 安全提醒
- 网友自建中转站，API 返回 `owned_by: "silievo"` 确认是第三方代理
- 套壳风险（记忆库 [[知识/中转站套壳识别]] 有实锤记录）
- 勿发敏感数据，key 定期轮换

## 改动的文件

- `C:\Users\Administrator\.claude\switch-api.js` — 新增 `moxingdazi` 预设（Base URL、模型、key、鉴权方式）
- `C:\Users\Administrator\.claude\settings.json` — 已切换为 moxingdazi 配置
- `C:\Users\Administrator\.claude\api-backups\settings-20260826-150405.json` — 切换前备份（DeepSeek 官方 #4）
- `C:\Users\Administrator\.claude\api-backups\settings-20260826-155610.json` — 切换前备份（火山 GA）
- `E:\第二大脑\日志\2026-08-26-进度.md` — 本日志

## 🤖 AI 会员拼车问答（纯咨询，无文件改动）

用户想了解「ChatGPT Plus 拼车」的完整知识：拼车是什么、怎么运作、价格、风险，以及自己适不适合拼车。

### 核心内容

- **拼车本质**：一个人开 Plus 号，把使用权分给多人，大家摊钱。本质是违反 OpenAI 使用条款的账号共享行为。
- **三种拼车模式**：① 共享号（最便宜，同一账号密码多人登录，对话完全公开）② Team 席位（卖家开团队套餐分席位，自己邮箱登录，对话相对隔离但管理员可见）③ 第三方平台（套壳型最危险，对话过平台服务器）
- **价格行情**（⚠️ 无法联网核实，基于常识）：共享号 25–60 元/月，Team 席位 50–100 元/月，官方 Plus 约 145 元/月
- **主要风险**：封号（OpenAI 条款禁止共享）、数据隐私（共享号对话全透明）、平台跑路、排队/掉线、最新模型限量
- **关键结论**：用户（有海外网络 + 想用自己的号 + 想用最新模型）不适合拼车，建议自己开号
- **无外币卡支付方案**：推荐美区 Apple ID + 礼品卡内购（最顺路径），其次虚拟信用卡、可信朋友代付

### 调研说明

- 本节调研全部失败（WebSearch 9 次 502、WebFetch 被网络拦截），除行情价和 GPT-5.6 状态外，原理和风险部分基于既有知识，基本可靠。

## 🔄 切回 DeepSeek 官方（中转站弃用，后悔药恢复）

用户决定弃用模型打子站中转，切回 DeepSeek 官方（最稳）。

- **恢复方式**：未用「后悔药恢复最近备份」（最近备份是火山 GA），改为恢复切中转前那份 `settings-20260826-150405.json`（= DeepSeek 官方 #4 配置），命令 `node switch-api.js restore settings-20260826-150405.json`（恢复前自动再备份 → `settings-20260826-191252.json`）
- **验证**：配置 6 个模型档位全 = `deepseek-v4-flash`；实测 `api.deepseek.com/anthropic/v1/messages` 返回 `model=deepseek-v4-flash`，HTTP 200，默认带 thinking
- **结论**：当前是 **flash**（不是 pro），全档位统一 flash，key = `sk-b7aa0c…`（官方 #4，08-18 起实际在用）

## 本次新增/改动的文件

- `C:\Users\Administrator\.claude\settings.json` — 已恢复为 DeepSeek 官方 #4 配置
- `C:\Users\Administrator\.claude\api-backups\settings-20260826-191252.json` — 恢复前自动备份（中转站配置）

## 待办

- [ ] 用户 `/exit` 重启 Claude Code，让 DeepSeek 官方配置生效
- [ ] 重启后 `/status` 确认 Base URL = `https://api.deepseek.com/anthropic`
- [ ] moxingdazi 中转站已弃用；若不再用可在对方平台撤销该 key

## 🎯 ChatGPT Plus 充值方案调研（港卡路线确定，待执行）

### 背景

用户想要 ChatGPT Plus（要最新模型 GPT-5.6、要充自己的账号、有海外网络）。三个诉求叠加后，排除拼车/代办/共享号，锁定"自己账号 + 官方订阅"路线。

### 调研过程（WebSearch 一度全 502，改走 GitHub 直连）

- 中转 WebSearch 多次 502 → 改用 `curl api.github.com` 搜前人仓库，读到 6 份直接相关：
  - `siuserxiaowei/chatgpt-recharge-guide-2026` — 2026 国内充值五路径：海外卡/虚拟卡/礼品卡/USDT/独立商城代办
  - `ZeroLu/online_payment_guide` — Wildcard 已失效，替代品 Bingocard（支持支付宝）
  - `zjvrny/virtual-card-pokepay` — WildCard 2025 跑路、DuPay 停、OneKey 凉、Fomepay 疑似跑路；存活：PokePay/VCard/RedotPay（多需 USDT）
  - `xFanly/VCC`、`AZeC4/ChatGPT-Plus`、`worrall339/ChatGPT` — 补充方案与虚拟卡现状
- 后切回正常 API，WebSearch 恢复，补查港卡开户 2026 政策 + USDT 虚拟卡闲置规则 + 港卡付 ChatGPT 手续费

### 关键调研结论

1. **虚拟卡平台死得快**（WildCard 跑路/OneKey 凉/等多平台暴雷），"先看活不活，再看贵不活"；现存平台 VCard/PokePay 基本都要 USDT
2. **USDT 虚拟卡不是永久有效**：部分平台 30 天不动自动删卡、有有效期/闲置费/维护费 → 结论"随用随充、卡内不留余额"
3. **港卡（中银香港/汇丰）付 ChatGPT 可行**：中银香港从内地中银转账免手续费，需信用卡（借记卡易失败），外币交易手续费约 2%；2026 年取消强制香港住址证明；见证开户成功率 72%-88%，部分要资产证明 5-50 万
4. **用户是深圳人** → 赴港办卡最方便（门槛低：中银香港 1000 港币起存、当场出卡），比"深圳见证开户"（要资产证明+等 7-15 天）更适合

### 决策（用户已定）

- **路线：赴港办中银香港卡**，用户有港澳通行证，需补有效签注
- 弃用 USDT 虚拟卡路线（对零基础太折腾、平台跑路风险、手续费叠加不省心）
- 用户已了解：拼车/代办不符"自己账号"诉求，排除

### 全局 CLAUDE.md 规则更新

在 `C:\Users\Administrator\.claude\CLAUDE.md` 调研节加了最高优先级规则：**不重复造轮子**——任何任务先找前人成果（GitHub/社区/记忆库），能借鉴先借鉴，禁止从零硬想。

## 本次追加的改动的文件

- `E:\第二大脑\日志\2026-08-26-进度.md`（本文件，追加本节省）
- `C:\Users\Administrator\.claude\CLAUDE.md`（调研节新增"不重复造轮子"最高优先级规则）
- `C:\Users\Administrator\.claude\projects\C--\memory\MEMORY.md`（记忆索引，同步待办）

## 待办（追加）

- [ ] 用户补港澳通行证有效签注（深圳出入境大厅，个人旅游签注，几天）
- [ ] 预约中银香港开户（官网/App，提前预约防白跑）
- [ ] 赴港材料：身份证 + 通行证（有效签注）+ 入境小白条 + 约 1000 港币起存
- [ ] 开卡后回内地，从内地中银转账到香港中银（免手续费），绑 ChatGPT Plus 订阅
- [ ] 可再帮用户查：深圳续签注详细步骤 + 中银香港预约流程（用户下次说"继续"就做）