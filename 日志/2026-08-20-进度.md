# 2026-08-20 进度

## 🎬 口播工作流 v1.8 验收通过（蘑菇气口修复 + 变焦运镜 + 前三秒特效音效 + 心跳音效）

### 验收跑通（python 驱动复刻 run.ps1，全流程零报错）
- 转录：195 字，funasr
- 分析：12 候选 → 10 cuts → 9 keep 段 → 成片 **32.886s**（旧版 42.78s / 16 段）
- 字幕：24 条
- 特效规划：3 特效（推拉/缩放）+ 1 心跳，`key_sentences_source=ai`
- 草稿导出：9 segments，720x1280，30fps

### 蘑菇气口修复确认（对比铁证）
- 旧版蘑菇区 2 条假剪切 `[47.34,47.52]` + `[48.12,48.6]`（掐「菇」尾音）
- 新版只剩 1 条真停顿 `[48.339,48.468]`（真实静音 0.43s，保留 0.3s 呼吸）
- 剪切点落在真实静音中段，不再掐尾音

### 特效音效落点
- 推拉@0.20s(hook 区) + 缩放@17.86s + 推拉@31.73s，全在 32.886s 内
- heartbeat@0.20s 音量 0.35，只在开头（前三秒主战场）

### ⚠️ run.ps1 再次丢失（第 2 次）→ 已恢复并双备份
- 8-19、8-20 两次丢失，原因不明（单文件消失，其余文件完好）
- 从会话 jsonl 提取 v1.7 完整内容恢复（UTF-8+BOM）
- 备份位置：
  1. 项目内 `_backup\run.ps1.v1.7.20260820.ps1`
  2. `E:\第二大脑\仓库\口播工作流备份\run.ps1.v1.7.20260820.ps1`（含说明.md，记录了丢失历史和恢复方法）

### 剪映草稿库已更新
- 新草稿已拷入 `C:\Users\Administrator\AppData\Local\JianyingPro\User Data\Projects\com.lveditor.draft\口播自动剪辑`
- 旧草稿备份为 `口播自动剪辑_旧版备份_20260820_105012`（保留了 8-19 的旧版备份）
- 用户下次开剪映可直接看「口播自动剪辑」

### 技术备忘（踩坑，避免下次再踩）
- **PowerShell 调中文路径有 GBK 编码问题**：`powershell -File` 传中文路径必挂（bash→powershell 参数编码乱）
- **git bash 的 /tmp 与宿主 Temp 视图不一致**：Write 工具写的临时 ps1、bash 里的 Windows 进程互相不可见/会消失，临时脚本方案不可靠
- **复刻 run.ps1 调用序列用 python 驱动最稳**：全部 UTF-8，无编码问题；用 Python311（funasr + pyJianYingDraft 都装在这）
- **验证 run.ps1 版本真伪**：检查是否含 `-Jianying`/`-Effects` 参数（备份目录的 run.ps1 是旧版勿用）

## 🎬 口播工作流 v1.9 黑绒被切修复（AI 层默认 KEEP + 真实静音校准）+ 特效音效停用

用户澄清「黑绒被切」= **内容被切**（有些句子直接被切掉，不是字尾气音）+ 气口不规范。根因：AI 层（DeepSeek）近义重复/口误判定过度激进，把通顺长句整段误删。同时用户明确不要推拉/缩放运镜（日常用关键帧），运镜+音效全停。

### 修复
- **AI 层默认 KEEP**（人工在 review.md 改 `[CUT]` 才删）：4 处误删消失（8 秒段「要你的老命/点赞收藏」、「兑一兑」、「二次加热了」、「但是啊我告诉你四种食物」）
- **全部 cut 边界统一真实静音校准**（safe_cut_range，找不到静音支撑就放弃剪切）
- **语气词只删独立停顿词**（前后都有 ≥0.3s 停顿），嵌句连读的「虽然啊」「但是啊」保留
- **edge_silence 专用校准**：不切首字/末字
- **特效音效停用**：`plan_effects.py` `DISABLED=True`，草稿 video+text 双轨、特效/音效素材 0

### 验收
- 分析 12 候选 → **5 cuts 全可信**（3 真停顿 + 开头 11.79s + 结尾 3.92s 静音）
- 成片 **47.57s**，DTS 非单调 **0/1426** ✅；字幕 39 条；剪映草稿 4 段、47.52s
- 验收脚本 bug 修复：`detect_silences` 对不存在文件返回 [] 会把 pause 候选全丢 → ffprobe `returncode!=0` 返回 None 回退

### 产物
- 带字幕成片：`C:\Users\Administrator\Documents\xwechat_files\wxid_ar8lbm6yzxlm22_d209\msg\video\2026-08\edit\final.mp4`（47.57s）
- 剪映草稿：`...\edit\jianying_draft\口播自动剪辑\`（4 段，无特效/音效轨）
- 审核稿：`...\edit\review.md`（5 条 CUT 全机械层，AI 层无候选）

## 🎬 video-cutter 字幕样式 v8（字号60 + 字幕比声音早修复）✅

用户要求：字号调 60 + 字幕不能比声音早。全部完成。

### 字号 20→60
- `srt_to_ass.py` 生成 ASS `FontSize=60`，MarginV=427 位置画面 2/3 处保留
- vision 目视确认：白字 SimHei，大小适中不超宽，50% 透明黑底保留

### 字幕比声音早修复（量化诊断 + 根因）
- **量化诊断**：对 32 条字幕逐条判断 start 时刻是否在语音中，发现 #25「不能二次加热。」start 比本句人声早 **650ms**（其余条 start 时人声已开始）
- **根因**：`srt_align.py` 的 `find_voice_start` 取窗口 `[ts-1.0, ts+0.8]` 内**最早**语音段，常误检到**上一句尾音**当本句起点 → 字幕 start 被提前顶到上句语音处
- **修复**（srt_align.py）：
  1. `find_voice_start` 改为按连续性分组语音段、取**离参考时刻最近**的语音段起点（参考时刻=FunASR 时间戳，偏晚、已落本句语音内，回溯段起点=本句真起点）
  2. `pre_read` 0.06→0（去掉提前显示）
- **验证**：#25 早 650ms 消失，仅剩最后一条「第四种」（成片结尾无语音边缘条，保持原值）；27/32 条时间被修正
- 成片：`D:\video-cutter\Projects\test_jinyun\output\带字幕_266fcfd9ef4b3c2ac7c0813706b4130a_v8.mp4`（49.06s/8.6MB）

### 待办
- [ ] 用户验收 v8 成片（断句/字号60/黑底/时间轴）
- [ ] 验收通过后把「srt_align + 断句(subtitle_from_transcript) + srt_to_ass + 烧录参数」固化进 `D:\video-cutter\scripts\all_in_one.py` 一键流程
- [ ] 之后再考虑音效/贴图/特效（用户明确顺序）

## 🎬 口播工作流 v1.9 剪映草稿库部署（v1.12 无特效新版）

- 旧版（v1.11 带特效）备份为 `口播自动剪辑_旧版备份_20260820_112501`
- 剪映当时在运行，`.locked` 锁文件被占无法删目录 → 直接覆盖草稿 JSON（draft_content.json + draft_meta_info.json），验证 tracks=video/text、特效/音效素材 0
- 待用户：关剪映重开刷新，打开「口播自动剪辑」验证无特效新版（5 cuts：3 停顿 + 头尾静音）

## 🔄 Claude Code 后端切换火山 Agent Plan（deepseek-v4-flash 正式版）

### 背景
用户要把 Claude Code 后端从 DeepSeek 官方切到火山方舟 Agent Plan 的 deepseek-v4-flash **正式版**（GA）。核心诉求：①配置不能搞坏现有 API；②正式使用亲自测速率（否决沙盒方案）。

### 已完成
- **switch-api.js v2.0**：`C:\Users\Administrator\.claude\switch-api.js`，命令 `volcengine|deepseek|backup|restore|list|status`，切前自动备份 + 恢复前双保险
- **桌面后悔药**：`C:\Users\Administrator\Desktop\后悔药.lnk`，双击一键恢复最近备份
- **实测打通**：Agent Plan 专属 key + `/api/plan` + `ark-code-latest` 模型 → HTTP 200，返回 `deepseek-v4-flash-ga-260731`（正式版）
- **fix API_KEY/AUTH_TOKEN 互斥**：切火山清旧 API_KEY，切回清旧 AUTH_TOKEN，杜绝"一换就坏"
- **记录**：`E:\第二大脑\项目\Claude Code 环境维护.md` v2.0（6 条踩坑）+ `系统\API密钥.md` 火山#3

### 关键踩坑（简版）
1. 普通 API key 调 `/api/plan` → 401；专属 key 要从 Agent Plan 订阅页「配置专属API Key」拿
2. Agent Plan 端点是 `/api/plan`，不是 `/api/coding`（那是 Coding Plan），也不是 `/api/v3`（会额外计费）
3. 模型必须填 `ark-code-latest` 路由名；直接填 `deepseek-v4-flash` 或 `-ga-260731` → 500
4. 模型列表里「DeepSeek-V4-Flash正式版」≠「DeepSeek-V4-flash」，要勾正式版
5. Harness 全部跳过（每个都是燃料值黑洞）；超额后付费不建议开

### 待办（下一步）
- [x] 用户执行 `node switch-api.js volcengine` + 完全重启 + `/status` 确认
- [ ] 实测速率后决定保留 or 切回
- [ ] 火山接入从待办清单划掉

## 🔄 火山 Agent Plan 实测更新（配置成功，但体验差 → 准备换回火山普通版）

### 配置成功（实锤）
- 用 `curl` 打 `/api/plan/v1/messages` 实测返回 `model=deepseek-v4-flash-ga-260731` —— `ark-code-latest` 路由别名底层就是 **deepseek-v4-flash 正式版(GA)**，非普通 flash ⭐
- `-ga-`=General Availability，`260731`=2026-07-31（DeepSeek 官方把 V4-Flash 从 Preview 升级为正式版）。GA 结构/参数量与 Preview 相同，仅官方重新后训练，Agent/代码能力更强，规格不变
- 默认开 thinking（返回带 `thinking` 字段）

### 体验差 → 决策：准备换回火山「普通版」
- **烧额度极快**：用户只问几句话就烧掉 **122/2000** 的套餐额度
- 大头原因：①调研派了一个子 agent，抓了 **46 次网页**（最大头）②上下文越聊越胖（每问一句重发全量历史）③模型默认开思考，先说一大段思考再回答
- **决定**：Agent Plan GA 版体验差，**换回火山平台的普通 `deepseek-v4-flash`（非 GA）**，仍走火山，不切回 DeepSeek 官方

### 新坑（避免再踩）
- **switch-api.js 只改磁盘 settings.json，不碰运行中会话进程已加载的 env** → 切后旧窗口仍带旧 key（`sk-`），打新地址（`ark-`）会被火山拒绝：`401 API key format is incorrect`。**这就是"这个会话死了、其他会话能用"的根因**（其他会话是切完才新开的新 env）。
- **修复**：切 API 后旧会话必须 `/exit` 完全重启才生效，在旧窗口里再跑脚本/登录没用。

### 待办
- [ ] 实测判断普通版 deepseek-v4-flash 是否走火山普通通道（非 Agent Plan 燃料）
- [ ] 把 `switch-api.js` 换成火山 → 火山普通版，重启验证
- [ ] 火山 GA 对比普通版结论沉淀（成本：官方表不区分预览/GA，官方口径同价）

## 🔄 最终决策：切回 DeepSeek 官方（2026-08-20 晚）

### 决策过程
用户反复纠结后最终拍板「切回 deepseek」。执行 `node switch-api.js deepseek` 完成切换，Base URL 回到 `https://api.deepseek.com/anthropic`，火山 Agent Plan 阶段结束。

### 关键结论（沉淀）
1. **火山普通 API（`/api/v3`）整体没有 Anthropic 协议**（实测 404），不是某款模型的问题，是"普通按量计费"这个服务类型没开 Anthropic 口子。Claude Code 接火山只能走 Agent Plan。
2. **燃料值烧太快是 Agent Plan 的硬伤**：40 分钟烧 201 AFP（5小时额度10%），未正式使用。烧的就是 deepseek-v4-flash 自己（key 权限+控制台路由都锁死 flash），不是 auto 换模型。
3. **`ark-code-latest` 路由别名底层 = deepseek-v4-flash-ga-260731 正式版(GA)**，GA 默认开 thinking，思考量是烧额度大头之一。

### ⚠️ 切回后发现 switch-api.js 隐患（待修）
- switch-api.js 的 `deepseek` preset 里写死的是**旧 key #1** `sk-66a20e...` + **主模型 pro**，但用户 08-18 起实际用的是 **#4** `sk-b7aa0c...` + **flash**。
- 切回后 settings.json 变成：key=`sk-66a20e...`(#1 旧)、主模型=`deepseek-v4-pro`——**不是用户之前用的配置**。
- 待修：把 switch-api.js 的 deepseek preset 更正为 #4 key + 全 flash。

### 待办
- [ ] 修正 switch-api.js deepseek preset（#4 key + flash 主模型），或用户重启前手动改回
- [ ] 用户完全重启 Claude Code 验证可用
