---
name: 2026-09-01-progress
description: 2026-09-01 项目推进记录
tags: [日志, 进度]
metadata:
  type: reference
  modified: 2026-09-01 23:25
---

# 2026-09-01 进度

## 口播 AI 剪辑流水线 v10.4

### 本次完成
- 在 `E:\项目\autoclip-web\core\jianying_exporter.py` 中新增 dB 音量规则：Peace BGM 默认 `-15dB`，并限制在 `-10dB ~ -25dB`；音效统一限制在 `-3dB ~ -10dB`。
- 将音效逻辑从“独立固定点”改为“强调事件驱动”：同一个强调事件同时生成特效字轨和音效轨，保证特效字与音效同起点绑定。
- 清单型口播支持相邻字幕取答案：例如“第二种就是”后接“咱们觉得非常珍贵的海鲜”时，强调字取“海鲜”，避免把“就是”误做大字。
- 增强 `E:\项目\autoclip-web\verify_current_draft.py`：新增 BGM dB、音效 dB、强调字段数、未绑定音效强调字段数验收。
- 改造 `E:\项目\autoclip-web\deep_dump_all_projects.py`：从写死 PID 改成自动扫描全部 `JianyingPro` 进程。

### 验收结果
- 生成剪映草稿：`C:\Users\Administrator\AppData\Local\JianyingPro\User Data\Projects\com.lveditor.draft\0901-1026`
- 字幕段数：`30`
- 文字轨数量：`5`
- 首句开始：`0.420s`
- 非零间隙数量：`0`
- 最大绝对间隙：`0 微秒`
- 浅黄色样式段数：`18`
- 其他非白色样式段数：`0`
- 音频素材数量：`5`
- 非剪映原生音频素材数量：`0`
- BGM 音量 dB：`[-15.0]`
- 音效音量 dB：`[-7.0, -5.0, -8.0, -8.0, -7.0, -8.0, -8.0, -6.0]`
- 强调字段数：`8`
- 未绑定音效的强调字段数：`0`
- 疑似机械断句数量：`0`

### 强调事件明细
- `0.420s`：姐妹们 + 亮一下 `-7dB`
- `1.020s`：千万不要 + 眼前一亮 `-7dB`
- `11.940s`：老命啊 + 大明王朝悬疑 `-5dB`
- `34.620s`：蘑菇 + 眼前一亮 `-8dB`
- `36.831s`：海鲜 + 眼前一亮 `-8dB`
- `40.265s`：芹菜 + 眼前一亮 `-8dB`
- `44.286s`：凉菜 + 眼前一亮 `-8dB`
- `46.321s`：您都记住了吗 + 仙尘音效 `-6dB`

### 当前卡点
- 用户确认剪映草稿 `8月13日`、`8月26日`、`8.12素材` 的绝大多数 `Timelines` 是单独成片，可以参考。
- 本轮自动扫描了当前 9 个 `JianyingPro` 进程，但 `deep_dump_all_projects.py` 未抓到可解析明文工程，结果为 `0`。
- 当前判断：这些 `Timelines/<UUID>/draft_content.json` 是剪映封装格式，不能直接按普通 JSON 解析；下一步要继续研究封装格式，或找到稳定方式让剪映把单条时间线明文加载到可读内存后再批量提取。

### 下一步
- P0：打开 `0901-1026` 草稿在剪映中人工观看，确认 BGM 不压原声、8 个强调字和音效在时间线上可见且可调整。
- P1：做“外部字幕优先”机制：如果素材旁有 `.srt` 或剪映导出的字幕文件，优先使用，解决 SenseVoice 偶发识别错词根因。
- P1：继续破解/提取参考草稿 `Timelines`，形成可复用的特效字、音效、字幕参数统计报告。
- P2：把强调事件规则接入工作台可调参数，让用户能在 Web 页面里调整 BGM dB、音效 dB、强调字数量和触发词。

## 口播 AI 剪辑流水线 v10.5

### 本次完成
- 按用户要求优先突破“查看原草稿样式和数据”，暂不继续改成片效果。
- 新增 `E:\项目\autoclip-web\extract_reference_style_data.py`，批量扫描剪映参考草稿 `8月13日`、`8月26日`、`8.12素材`。
- 生成参考样式资料库 `E:\项目\autoclip-web\reference_style_data\`，包含时间线索引、明文子草稿清单、文字样式、轨道样式、key_value 音频资源、key_value 特效资源和中文报告。
- 修复 `E:\项目\autoclip-web\inspect_jianying_crypto.py` 的 URI 输出，`crypto_key_store.dat` 现在能显示密钥对应的组合音频、预渲染视频、音频算法资源路径。

### 验收结果
- 时间线总数：`25`
- 疑似未剪完时间线：`1`
- 可扫描明文文件：`70`
- 可解析明文 JSON：`70`
- 明文子草稿：`47`
- 文字素材样本：`82`
- 轨道片段样本：`189`
- key_value 素材索引：`1037`
- key_value 音频资源：`128`
- key_value 特效资源：`49`
- 主要字体分布：`字语咏楷体 27`、`方正风雅宋 27`、`俪金黑 10`、`三极古拙楷书 6`
- 主要音频资源：`轻音乐（舒缓钢琴曲）`、`轻音乐（释怀）`、`一声亮玲`、`紧迫`、`Peace`、`仙尘音效`、`Ding，可爱提示音`、`亮一下`、`眼前一亮`
- 脚本语法检查：`python -m py_compile extract_reference_style_data.py inspect_jianying_crypto.py` 通过。

### 当前判断
- 已经能稳定查看和复用参考草稿的花字、字幕外观、字体、颜色、位置、缩放、素材选择偏好。
- 主时间线 `draft_content.json` 仍是剪映加密/封装内容，完整成片节奏、BGM/音效真实轨道打点和音量还没完全读出。
- 本机剪映安装目录已确认是 `D:\JianyingPro\11.3.0.14362\`；未发现可直接安全调用的公开解密命令行入口，线索集中在 `videoeditor.dll` 等二进制内部能力。

### 下一步
- P0：基于 `reference_style_data\text_style_samples.json` 和 `track_style_samples.json`，把花字样式参数接入当前导出器的可配置样式库。
- P1：继续攻主时间线解密/内存明文入口，目标是读出完整 BGM/音效轨道打点和真实音量。
- P1：将 `key_value_audio_assets.json` 里的高频音效资源用于声音设计规则优化，减少凭空选音效。

## Windows 自动重启修复

- 用户反馈电脑仍持续自动重启，并且弹出 Windows 找不到 `D:\搜狗输入法\SogouInput\Components\IChat\1.0.2.3232\SOGOUSmartAssistant.exe`。
- 已恢复 `SOGOUSmartAssistant.exe`，并恢复此前隔离的 `SogouCloud.exe`、`PinyinUp.exe`、`SogouComMgr.exe`、`isgpet.exe`，避免继续出现“找不到文件”弹窗。
- 复查日志确认 2026-08-31 18:30 仍为 `lsass.exe + RPCRT4.dll + 0xc0000005` 崩溃触发 `wininit.exe` 强制重启。
- 已用 DISM 回滚高疑更新包 `Package_for_RollupFix~31bf3856ad364e35~amd64~~26100.9168.1.19`，命令输出 `The operation completed successfully`。
- 回滚后 `KB5121003` 已从 `Get-HotFix` 消失，系统存在 `RebootPending`，需要正常重启一次完成卸载。
- 已临时关闭自动更新安装：`HKLM:\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate\AU\NoAutoUpdate = 1`。
- 修复备份目录：`C:\Users\Administrator\Documents\Codex\2026-08-31\new-chat-2\work\windows-fix-20260831-165650`。

## 卡包决策样本归档

### 本次完成
- 按 `E:\第二大脑\CLAUDE.md` 和各目录说明读取分类规则后，判断本次“决策样本记录”主要价值是长期辅助 AI 理解用户决策方式，归入 `知识/`，不新建项目。
- 新建 `E:\第二大脑\知识\用户决策样本.md`，记录卡包是否有必要的决策背景、候选方案、真实权重、观点变化、伪需求、心理动机和后续 AI 辅助策略。
- 更新 `E:\第二大脑\知识\用户人物画像.md`，新增一条高浓度摘要和样本链接，版本 `v1.5` → `v1.6`。
- 更新 `E:\第二大脑\MEMORY.md` 总索引，加入该决策样本入口。

### 分类结论
- 不创建独立项目：这不是后续要持续推进的具体任务。
- 不只写进日志：日志只能记录今天发生了什么，不能承载长期可复用画像。
- 新建独立知识文件：一次完整决策样本较长，直接塞进 `用户人物画像` 会让主画像臃肿；主画像只保留摘要和链接。

## 招商银行一卡通万事达人民币借记卡

### 本次完成
- 用户确认：招商银行一卡通万事达人民币借记卡已办完。
- 已回写 `E:\第二大脑\日志\2026-08-27-进度.md`，把“用户去招行办「一卡通万事达人民币借记卡」（进行中）”更新为已完成。

### 记录边界
- 只记录用户明确确认的“已办完”。
- 未记录卡面、费用、是否开通境外网上支付、是否存款、是否已绑定 Google Play 或其他功能状态。
