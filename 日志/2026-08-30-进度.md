---
name: 2026-08-30-progress
description: 2026-08-30 工作进度记录
tags: [进度]
metadata:
  type: reference
  modified: 2026-08-30 15:10
---

# 2026-08-30 进度

## 动物大战 2.0 重制启动

- 读取 `E:\第二大脑\项目\游戏\动物大战.md`，确认旧版主线已到 v4.0，根目录 `index.html` 保留为旧版。
- 核对 GitHub 仓库 `z15314102792-arch/animal-battle` 和本地仓库 `E:\项目\animal-battle`，从 `master` 新建分支 `v2-battle-director`。
- 在 `E:\项目\animal-battle\v2\` 新增 2.0 原型，方向是“生态策略乱斗 / Battle Director”，不是动物版 MOBA。
- 已实现 3v3 河谷 MVP 骨架：选队、部署、本能策略、空间层、自动 AI、战术指令、胜负判定、事件日志。
- 已新增设计文档 `E:\项目\animal-battle\v2\docs\design-v0.1.md`，记录旧版画像、2.0 支柱、MVP 动物、空间层、本能、战术指令和经济系统预留约束。
- 已新增新版测试：`test:v2` 逻辑冒烟测试和 `test:v2:visual` 视觉冒烟测试。
- 修复旧版 Puppeteer 测试写死 Chrome 路径的问题，改为自动寻找 Chrome 或 Edge。
- 验证结果：旧版验收 5/5，通过；旧版冒烟 21/21，通过；新版冒烟 18/18，通过；新版视觉截图桌面/手机均生成，Canvas 非空比例 1。

## 下一步

- 给 2.0 增加战斗复盘：自动提取 2 到 3 个关键因果。
- 扩展行为标签：群居、伏击、护崽、食腐、逃跑。
- 做生态导演原型：根据紧张度和生态失衡度触发天气、迁徙或第三方事件。

## 🖥️ Windows 强制重启排查与处理

- 用户反馈电脑第二次弹出“电脑出现了一些问题，将在一分钟后重启”。
- 已检查 Windows 系统日志、应用程序日志、可靠性记录、Windows 错误报告、崩溃转储目录、驱动与安全软件状态。
- 直接原因确认：两次都是 `lsass.exe` 崩溃后由 `wininit.exe` 触发系统保护性强制重启，不是普通 Windows 更新重启。
- 两次关键时间：
  - 2026-08-29 19:18:01：`lsass.exe` 在 `RPCRT4.dll` 中崩溃。
  - 2026-08-30 14:19:59：`lsass.exe` 在 `RPCRT4.dll` 中再次崩溃。
- 关键错误特征：异常代码 `0xc0000005`，故障模块 `C:\WINDOWS\SYSTEM32\RPCRT4.dll`，强制重启记录显示 `lsass.exe` 意外终止，状态码 `-1073741819`。
- 崩溃转储文件已确认存在：
  - `C:\Windows\System32\config\systemprofile\AppData\Local\CrashDumps\lsass.exe.1356.protected.dmp`
  - `C:\Windows\System32\config\systemprofile\AppData\Local\CrashDumps\lsass.exe.1352.protected.dmp`
- 已排除的方向：不是 Codex 触发，不是普通更新重启；`DISM /CheckHealth` 未发现组件存储损坏，`SFC` 记录显示校验完成；`lsass.exe` 已加载模块里未发现第三方 DLL 直接注入。
- 主要怀疑方向：Windows 25H2 / Build 26200.9168 与 360 安全卫士内核级驱动存在兼容冲突。证据包括多项 360 驱动运行、`360Hvm` 驱动未加载、多个 360 文件系统筛选器已加载、Microsoft Defender 被关闭。
- 用户后续已自行处理：卸载 360 安全卫士、卸载 360 看图，并开启 Windows“内存完整性”。
- 当前建议：观察 24 小时是否复现；如果仍复发，再用 WinDbg 分析上述 `.dmp` 转储文件，定位更深层调用栈。

## 🪟 Windows 黑框一闪排查

- 用户反馈经常出现类似终端的黑框，浮现不到一秒后消失。
- 已检查自启动项、启动文件夹、计划任务、PowerShell 日志、进程创建日志、预读取记录、当前进程、服务、驱动和文件系统筛选器。
- 初步结论：黑框一闪更像是自启动项、计划任务、更新器或脚本短暂拉起命令行窗口，不像是 `lsass.exe` 崩溃本身。
- 已发现并处理 360 画报自启动残留：
  - 残留程序：`C:\Users\Administrator\AppData\Roaming\360huabao\360huabao.exe`
  - 已删除自启动项：`HKCU:\Software\Microsoft\Windows\CurrentVersion\Run\360huabao`
- 已发现 360 卸载后仍有底层残留：
  - `360AntiAttack`
  - `360AntiHijack`
  - `360AntiSteal`
  - `360Camera`
  - `360FsFlt`
  - 文件系统筛选器里仍有 `360AntiSteal`、`360Box64`、`360FsFlt`
- 当前判断：这些 360 驱动可能需要重启后才会卸载干净；若重启后仍存在，说明 360 没卸干净，需要继续做残留清理。
- 可能造成黑框一闪的计划任务来源：
  - WPS 更新任务：`WpsUpdateTask_Administrator`、`WpsUpdateLogonTask_Administrator`、`WpsWakeWnsLogonTask`，动作显示为 `by user disabled`，但仍有触发记录。
  - Google 更新任务：`GoogleUpdaterTaskUser152.0.7933.0{5FAED628-4DF1-4A86-9D8F-618BCCFCEC6E}`，动作也显示为 `by user disabled`。
  - Windows 热补丁监控：`C:\Windows\System32\cmd.exe /d /c C:\Windows\System32\hpatchmonTask.cmd`。
  - 抖音守护任务：`D:\douyin\douyin_guard.exe --start-from=taskschd`。
- 已开启后续抓证据日志：
  - 计划任务详细日志：`Microsoft-Windows-TaskScheduler/Operational` 已启用。
  - 进程创建命令行记录：`HKLM\Software\Microsoft\Windows\CurrentVersion\Policies\System\Audit\ProcessCreationIncludeCmdLine_Enabled = 1`。
- 下一步建议：重启电脑后复查 360 驱动是否仍在运行；如果黑框再闪，查看新开启的计划任务日志和进程创建日志，定位具体程序。

## 改动的文件

- [[日志/2026-08-30-进度]]：追加 Windows 强制重启排查与处理记录。
- [[知识/Windows强制重启排查]]：补充黑框一闪、计划任务和 360 残留排查要点。
- [[MEMORY]]：补充 2026-08-30 进度索引。

## 待办

- 观察卸载 360 与开启内存完整性后，是否继续出现“一分钟后重启”。
- 若再次复发，优先分析 `lsass.exe` 崩溃转储文件，并复查 360 驱动是否有残留。
- 重启电脑后复查 `360AntiAttack`、`360AntiHijack`、`360AntiSteal`、`360Camera`、`360FsFlt` 是否仍在运行。
- 如果黑框一闪再次出现，读取新开启的计划任务详细日志和进程创建命令行记录，确认具体来源。

## 动物大战 2.0 v0.2 多界面与公网入口

- 根据用户反馈继续推进《动物大战 2.0》：要求其他设备和网络也能玩、提供入口、不要像浏览器长页面一样上下滑、增加多界面跳转、开始后切换到更清晰的对战场景、动物重做并更有标志性。
- 在 `E:\项目\animal-battle\v2\index.html` 完成 v0.2 多界面应用壳：入口、组队、部署、战斗、复盘五个底部跳转界面。
- 开始战斗后自动切换到战斗屏，Canvas 战场作为主区域，右侧只保留存活、指令、时间、战术按钮和事件日志。
- 新增 `E:\项目\animal-battle\v2\tokens.css`，抽出颜色、字体、间距、动效时间等设计令牌。
- 动物表现从 emoji 头图改为 Canvas 标志化立体轮廓：公牛弯角、鹰展开翼、乌龟壳纹、母鸡冠与喙、鳄鱼长吻、蜘蛛八足。
- 更新 `test/v2.smoke.test.js`：新版冒烟测试扩展到 20 项，检查底部跳转、开始后自动进入战斗画面。
- 更新 `test/v2.visual.test.js`：覆盖桌面、320、375、414、768 宽度，并检查页面高度等于视口高度，避免整页滚动。
- 验证结果：旧版验收 5/5，通过；旧版冒烟 21/21，通过；新版冒烟 20/20，通过；新版视觉测试 5 个视口通过，Canvas 非空比例 1。
- 已合并到 `master` 并推送，公网入口已确认 HTTP 200：`https://z15314102792-arch.github.io/animal-battle/v2/`，标题为“动物大战 2.0 v0.2”。

## 动物大战 2.0 v0.3 生态导演与因果复盘

- 继续推进《动物大战 2.0》全面重制，旧版根目录 `index.html` 保留不动，新版仍在 `E:\项目\animal-battle\v2\`。
- 更新 `E:\项目\animal-battle\v2\index.html` 到 v0.3：新增行为标签库，每只 MVP 动物拥有 2 个物种行为标签，例如破阵、空袭、锚点、护巢、伏击、控场、护群、水栖。
- 新增生态导演原型：战斗中按时间和紧张度触发河谷事件，第一批为暴雨涨水、谷雾压低、迁徙影子。
- 生态事件已进入实际规则：暴雨影响水域移动，谷雾影响远距离索敌，迁徙影子增强群居贴队友倾向；事件不会直接给某一方加伤害或改胜负。
- 新增战斗统计：单位记录输出、承伤、击杀、控制次数、水域停留时间，战术指令次数进入复盘统计。
- 复盘界面从普通日志升级为因果解释，结束后生成 2 到 4 条关键原因，覆盖最高输出、控制贡献、河谷空间层、生态事件和玩家指令。
- 更新 `E:\项目\animal-battle\test\v2.smoke.test.js`：新版冒烟测试从 20 项扩展到 28 项，覆盖行为标签、生态导演、战术指令统计和复盘生成。
- 更新 `E:\项目\animal-battle\v2\docs\design-v0.1.md`：追加 v0.3 系统调整与设计约束。
- 验证结果：旧版验收 5/5，通过；旧版冒烟 21/21，通过；新版冒烟 28/28，通过；新版视觉测试 5 个视口通过，Canvas 非空比例 1，页面高度均等于视口高度。
- 已提交并推送项目仓库，提交号 `9281857`，提交信息 `v2: 加入生态导演与因果复盘`。
- 公网入口仍为 `https://z15314102792-arch.github.io/animal-battle/v2/`，等待 GitHub Pages 缓存刷新后应显示 “动物大战 2.0 v0.3”。
