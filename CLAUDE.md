---
name: ""
tags: [入口]
metadata: 
  node_type: memory
  originSessionId: 53099e30-6fac-4ddc-b8d1-0dd8bee1197a
  modified: 2026-09-13
  version: v4.19
---

# 第二大脑 · AI 操作手册

> 版本 v4.19 | 2026-09-13

## 这是什么

这是一个 **Obsidian 知识库**（也叫"第二大脑"或"记忆系统"），用来持久化保存跨会话的知识。
你是维护这个知识库的 AI 助手，下面是你需要遵守的规则。

## 最高优先级工作原则

### 路径与规则唯一性（最高优先级）

第二大脑的**真实目录与当前 `CLAUDE.md` 是保存路径的唯一权威来源**。任何方法论、旧日志、历史会话、其他 AI 写入说明，都不能自行覆盖当前目录。

1. **唯一主库**：知识库本体只有 `E:\第二大脑`。Google Drive 不承担第二套知识库、项目区、备份区或资料区，它只有一个作用：作为 ChatGPT 可以读写 `E:\第二大脑` 的同步介质。
2. **唯一 Drive 入口**：通过 Google Drive 操作第二大脑时，只允许进入 `我的笔记本电脑/第二大脑/`（即 `E:\第二大脑` 的电脑同步目录），所有相对路径必须与 E 盘主库一致。禁止在 My Drive 根目录、`ChatGPT/`、`02_项目资料/`、`03_第二大脑备份/` 等位置另存知识库内容。
3. **写入前先寻址**：先读当前 `CLAUDE.md` + `MEMORY.md`，再检查 `我的笔记本电脑/第二大脑/` 中实际存在的目录和同主题文件；已有文件优先更新，禁止因为没先查到就新建同类文件。
4. 当前允许的正式顶层分类只有：`项目/`、`知识/`、`技能/`、`资料/`、`日志/`、`仓库/`、`待办/`、`灵感/`、`_垃圾站/`、`系统/`（另有工具目录如 `scripts/`、`.obsidian/`）。**未经用户明确批准，不得新建新的顶层知识分类。**
5. `PARA`、`CODE`、`Projects/Areas/Resources/Archives` 等只可作为方法参考，**不是当前物理目录名**。禁止自动创建 `01 Projects`、`02 Areas`、`03 Resources`、`04 Archives`、`日记/` 等旧式/外部目录。
   - 已于 2026-09-13 实测该入口双向同步：本地主库新建文件出现在此镜像，云端修改同一文件回到 E 盘。原生 Google 文档必须先全文转为正确目录的 Markdown 并读回核对，才可删除原件；不得把 `.gdoc` 快捷文件视为正文已入库。
6. 若当前环境无法访问 `我的笔记本电脑/第二大脑/` 或无法确认其与 `E:\第二大脑` 的同步关系，必须明确说明“当前不能安全写入第二大脑”，不得改存其他 Drive 位置后声称已保存。
7. 规则冲突时优先级固定为：**`E:\第二大脑` 实际结构 / `我的笔记本电脑/第二大脑` 同步结构 + 当前 `CLAUDE.md` > `MEMORY.md` 索引 > 当前项目文件 > 技能/资料中的方法论 > 历史日志/会话**。

### 保存与写入复核

- **日志必须完整**：进度日志要能还原做了什么、为什么做、改了哪些文件、当前状态和下一步。
- **写入前必须复核**：最终写入第二大脑和 git 提交前，核对事实、真实目标路径、是否已有同主题文件、是否误建新目录、敏感信息和暂存文件。
- 不再使用 0-4 级任务分级，也不把模型选择、额度优化或“谁当下级”写成知识库全局规则；这些按当前任务临时决定。

### 不重复造轮子

任何任务都先找前人成果，再决定是否自己做。这里的“前人成果”包括 GitHub 开源项目、社区教程、成功案例、同类产品、第二大脑已有记录、历史项目和踩坑日志。

执行要求：

1. 调研信息、找灵感、做项目、修 bug、解决卡点，都先搜索已有方案。
2. 能直接复用的，优先复用；能借鉴结构、流程、代码、提示词、UI、部署方式的，先借鉴。
3. 方案里写明参考来源和借鉴点，避免凭空重新设计。
4. 只有确认没有合适案例，或现成方案不适合用户环境、成本、能力门槛时，才自己创造。
5. 遇到卡点不要反复微调硬试，先搜别人怎么解决。

一句话：**先抄成熟作业，再做必要改造；不要每次从零发明。** 详细原则见 [[知识/调研先行]]。

## 目录结构

```
E:\第二大脑\
├── MEMORY.md          ← 总索引（每次会话自动加载）
├── CLAUDE.md          ← 本文件（AI 操作手册）
│
├── 💡 灵感/           ← 突然冒出的点子，还没想清楚要不要做
├── ✅ 待办/           ← 定下来要做、但还没开始的清单
├── 🎯 项目/           ← 正在做 / 做过的具体事
│   └── 游戏/          ←   做的小游戏
├── 🧠 知识/           ← 记住的道理、规则、教训、结论
├── 🛠️ 技能/           ← 「照着步骤就能做」的操作指南
├── 📚 资料/           ← 某个工具怎么装、怎么配、参考信息
├── 📅 日志/           ← 每天发生了什么（进度流水账）
│   └── AI日报记录/    ← 早报/日报（信息聚合类）
├── 📦 仓库/           ← 废弃的、备份的、历史基线（不常用但留着）
│   └── Claude Code 历史/  ← 历史会话存档
├── 🔐 系统/           ← 密钥等敏感文件（已 gitignore，不能外传）
├── 🗑️ _垃圾站/        ← 不要的内容，先丢这（下划线开头，自动排最下面）
├── scripts/           ← 脚本目录（daily_analyze.py 日报、phone-setup.sh 手机配置）
├── index.html         ← 知识库入口网页
│
└── .obsidian/         ← Obsidian 配置（不要改）
```

## 分类逻辑（最重要，先背下来）

**按「这个文件现在要拿来干嘛」分，而不是看它叫什么名字。**

拿到一个文件，问自己一句：**「现在要拿它干嘛？」**

| 答案 | 放哪 |
|------|------|
| 这个我要动手做 | 🎯 项目/（游戏放 项目/游戏/） |
| 以后都用得上的结论、道理、教训 | 🧠 知识/ |
| 照着步骤就能操作 | 🛠️ 技能/ |
| 查某个工具的信息 | 📚 资料/ |
| 查某一天的行业资讯（早报/日报） | 📅 日志/AI日报记录/ |
| 记录某一天发生了什么 | 📅 日志/ |
| 不常用了，但先留着 | 📦 仓库/ |
| 还没想清楚要不要做的点子 | 💡 灵感/ |
| 定下来要做的清单 | ✅ 待办/ |
| 密钥等不能外传的 | 🔐 系统/ |
| 没用了、先丢这的 | 🗑️ _垃圾站/ |

**边界判断：**

- **知识 vs 技能**：知识是「结论」（为什么、是什么），技能是「步骤」（怎么做）。
- **技能 vs 资料**：技能是通用方法（谁都能用），资料是某个具体工具的配置（手机Claude 怎么装、CCE 怎么配）。
- **项目 vs 仓库**：项目还在推进或刚做完 → 项目/；做完不玩了、废弃了 → 仓库/。
- **项目 vs 待办**：已经开始动手 → 项目/；只是列了个计划还没动 → 待办/。
- **知识 vs 日志**：知识是「长期有效的结论」，日志是「某一天发生的事」。别把当天的流水账写进知识。
- **日志/ vs 日志/AI日报记录**：当天的「事」（进度）→ 日志/；早报/日报等「信息聚合」→ 日志/AI日报记录/，**不放 日志/ 根目录**。

## 文件命名

- **所有 .md 文件用中文命名**（Obsidian 图谱直接显示文件名）
- 日志文件用日期格式：`YYYY-MM-DD-进度.md`（进度）；早报/日报放 `日志/AI日报记录/`，命名 `YYYY-MM-DD-AI日报.md`
- 项目/知识/技能/资料文件用中文名，一眼看懂：`中国象棋.md`、`手机Claude.md`

## Frontmatter 规范

每个记忆文件开头必须有 YAML frontmatter，包含：

```yaml
---
name: <短横线连接的英文slug>
description: <一句话描述，用于 AI 判断相关性>
tags: [标签1, 标签2]            # 可选，Obsidian 图谱用，必须放顶层！
metadata:
  type: user | feedback | project | reference  # 必填
  modified: <日期或时间戳>                      # 每次修改必须更新
---
```

> ⚠️ **tags 必须放 frontmatter 顶层**（和 name/description 同级），不能嵌套在 metadata 里面。Obsidian 只认顶层 tags，嵌套的不生效。

项目类文件额外包含：
```yaml
metadata:
  type: project
  status: 活跃 | 稳定 | 待修 | 废弃方案 | 运行中 | ...（可自定义，优先具体描述）
  version: vX.Y（如果有）
```

## 链接格式

- 使用 Obsidian wikilink：`[[路径/文件名]]`（不带 .md 后缀）
- 链接不存在 → 视为"待创建"，可以后续补
- MEMORY.md 中的每个条目格式：`- [[路径/文件名]] — 一句话说明`

## 你的职责

### Obsidian 图谱颜色约定（严格遵守）

| 颜色 | 匹配规则 | 目录 | 含义 |
|------|----------|------|------|
| 🟣 紫 | `tag:#入口` | 根目录 | 入口文件（CLAUDE.md、MEMORY.md） |
| 🟢 绿 | `path:项目/` | 项目/ | 正在做/做过的 |
| 🩵 青 | `path:知识/` | 知识/ | 结论、规则、教训 |
| 🟠 橙 | `path:技能/` | 技能/ | 操作指南 |
| 🔵 蓝 | `path:资料/` | 资料/ | 工具配置 |
| 🩷 粉 | `path:日志/` | 日志/ | 每日进度 |
| 🟡 金 | `path:仓库/` | 仓库/ | 废弃 + 备份 |
| 💜 浅紫 | `path:灵感/` | 灵感/ | 点子 |
| 🟤 棕 | `path:待办/` | 待办/ | 待办清单 |
| ⚫ 深灰 | `path:系统/` | 系统/ | 密钥（敏感） |
| 🗑️ 暗红 | `path:_垃圾站/` | _垃圾站/ | 不要的内容 |
| 🔘 灰 | `tag:#废弃方案` | 任意目录 | 废弃方案（覆盖任何目录色） |

**铁律：**
- `graph.json` 只在本机 PC 生效，**不同步 git**（手机/电脑互相覆盖会冲突，2026-08-12 已踩坑）。手机端图谱用默认灰色。
- 每个目录都必须有颜色。新增目录时 → 同步更新 `graph.json` 和上表。
- 任何文件都不能是黑色（系统/ 例外，但系统/ 只有密钥一个文件，通常被 gitignore 不显示）。
- 分类按**人看得懂**的维度：这是干嘛的 → 还能不能用。

### 每次会话结束时（自动）
1. 更新 `日志/当天日期-进度.md`：追加今日完成的事项
2. 更新涉及到的对应记忆文件：版本号、改动摘要、待办
   - 游戏 → `项目/游戏/xxx.md`
   - 其他项目 → `项目/xxx.md`
   - 结论/规则 → `知识/xxx.md`
   - 操作步骤 → `技能/xxx.md`
   - 工具配置 → `资料/xxx.md`
3. **提交前自检**：
   - `git add 灵感/ 待办/ 项目/ 知识/ 技能/ 资料/ 日志/ 仓库/ _垃圾站/ scripts/ .github/ CLAUDE.md MEMORY.md .gitignore index.html .obsidian/app.json .obsidian/appearance.json .obsidian/core-plugins.json .obsidian/community-plugins.json`
   - ⚠️ 禁止 `git add -A`——会混入第三方软件自动生成的未审核文件
   - ⚠️ `系统/` 目录**不要**加入 git add 清单（里面有密钥，已用 .gitignore 拦截，但白名单里也别写，双保险）
   - `git diff --stat --cached` 确认暂存区无误
   - **同步顺序（2026-08-15 起）**：先 `git pull origin master`（拉云端最新，避免覆盖其他设备改动）→ 再 `git commit -m "..."`（提交本地改动）→ 最后 `git push origin master`（推送）
   - 若 pull 时遇到冲突，停下来向用户说明再处理，不要硬来
   - origin → GitHub
### 每周一次（建议周日晚上）
4. 运行 `/记忆体检` — 检查知识库健康度
5. 运行 `/记忆周报` — 生成本周总结

### 交付文件绝对路径规范（2026-09-12 长期规则）
- 每次生成 APK、Web 构建包或其他需要用户手动打开/安装的文件，最终报告必须同时给出 Windows 完整绝对路径（例如 `E:\项目\deep-mine\builds\android\deep-mine-a0.1.apk`），**严禁只写文件名或相对路径**。
- 需要用户手动安装时，必须明确写：“你现在安装这个文件：完整绝对路径”；
- 需要用户手动打开时，必须明确写：“你现在打开这个文件：完整绝对路径”。

### 用户说"保存进度"时
6. 立即执行本次内容归档、当天日志和可用的同步步骤，不等会话结束。每周体检/周报不随每次保存自动运行。日志按当前北京时间完整日期 `YYYY-MM-DD` 命名，禁止写死年份、月份或沿用旧日期示例。
7. **保持保存流程简单可解释**：先按下面两种环境选择实际可执行流程，不把本机工作环境的能力套给普通 GPT 对话。
8. **禁止日常保存手工构造 Git 底层对象**：除非常规文件级提交方式不可用且确有必要，否则不得为了批量提交自行做 Base64 编码、创建 Blob/Tree、手工更新 ref。若只能走这类复杂底层流程，先停下向用户说明，不得静默继续。

#### 本机工作环境保存

读取 E 盘当前手册、总索引及同主题文件 → `git pull origin master` → 基于最新版更新对应笔记及版本，追加当天日志，必要时更新索引 → 核对事实、目标路径、引用和本次改动 → 只暂存本次白名单文件并提交/推送 → 等待 Drive 同步，读回本次云端正文，与 E 盘核对。

#### 普通 GPT 对话通过 Drive 保存

**先区分聊天记忆与主库入库（2026-09-13更正）：**用户说“保存进度”指保存到第二大脑，不是只更新ChatGPT记忆。出现“记忆已更新”或回复了下次继续位置，均不构成主库保存证据。手册文件更新不会自动改变已打开普通对话的指令；先明确调用Google Drive插件读取本镜像当前手册和索引。安装或连接后，旧会话未取得工具时，按官方说明新开普通对话并明确使用插件；不要求改成工作模式。仍无法读取镜像、生成可上传的Markdown文件或更新原始正文时，如实报告未入库。

1. 普通对话也可使用插件，但须先确认当前会话实际具有原始文件上传/内容更新工具及权限；只读搜索或Google Docs编辑能力不等于能保存Markdown。
2. 从唯一入口 https://drive.google.com/drive/folders/1Vsvl-fe0rmC4l6gL_12lgi-CuSTA18BP 读取当前 `CLAUDE.md`、`MEMORY.md`、已有目录和同主题文件，使用与 `E:\第二大脑` 一致的相对路径。
3. 已有文件读取最新版后更新同一原始 `.md` 文件，保留原内容和文件身份；更新版本/修改日期。日志只追加。确需新文件先查重，明确目标父目录，不能默认上传到Drive根目录。
4. 写入后重新读取本次云端文件，核对正文、位置、引用和原有内容是否保留；工具返回成功、文件名存在或改了元数据都不等于正文核验通过。
5. 由 Google Drive Desktop 同步回 E 盘；只有实际核对 E 盘正文后才报告“本地主库同步已验证”。当前普通对话不能访问 E 盘时，报告“云端写入并读回通过；E 盘同步待核实”，不得冒称全部保存完成。
6. GitHub备份单独报告：普通对话有可用文件级GitHub工具时基于最新远端更新并核验；否则报告未执行。不能假装在普通对话运行本机终端，也不能把Drive同步当作GitHub已提交。
7. 没有原始Markdown写入能力或唯一镜像不可访问时，明确报告不能安全入库，不改存Google Docs、Library、ChatGPT记忆或其他Drive目录后声称完成。原生Google文档必须全文转换、正确入库并核对后才视为已保全。

工具发现时须检查当前实际参数：原始文件上传为 `google_drive_upload_file`，已有正文更新为 `google_drive_update_file` 且须传有效文件内容引用 `file_uri`；不传正文引用只改元数据。工具可用、可生成上传文件及服务权限是三项不同条件。本机出现这些工具不证明另一普通对话已加载或可执行；只有该普通对话实际写入并读回，才能确认其云端入库通过。

#### 每次保存的完成证据

分别列出“E盘写入/同步”“Drive写入/读回”“GitHub提交/推送”状态、本次文件完整路径和未完成原因。没有验证的项目标为待核实，失败步骤标为失败，不用一句“已保存”合并掩盖。电脑离线、同步暂停、权限变化或多会话同改时以实际证据为准，不承诺即时同步或永不出错。普通对话使用插件的官方依据：https://learn.chatgpt.com/docs/plugins 。

### 多会话协作保存规范（2026-08-20 新增）

多个会话并行时，**每个会话都必须保存自己的工作**（各会话内容不同，只存一个会丢其他会话）。为避免互相覆盖：

1. **错峰**：一个会话保存 + git 推完，另一个再保存；避免同一时刻写同一文件
2. **日志一律追加**，不整篇重写（Edit 追加，保留已有内容）
3. **git 串行**：同一仓库不能两进程同时 push；push 失败先 `git pull` 再推
4. **遇「检测到其他改动」**：说明有其他会话在改同一文件 → **重新读取最新版再编辑**（追加式），不要基于旧版本覆盖
5. **先 pull 再写**：写入前先 `git pull origin master`，确保基于最新版本

## 判断属于哪个项目

按优先级：
1. **文件路径**：改了哪个目录下的文件
2. **对话关键词**：用户提到的项目名
3. **纯分析讨论**：根据对话主题匹配
4. **判断不了就问用户**

## 维护原则

- **新增前先查重**：检查是否已有类似文件，更新旧文件而非创建新的
- **过时标记**：信息过时了不要删，在 frontmatter 加 `status: archived`，正文保留
- **矛盾标记**：发现两个文件说法矛盾，在两者正文末尾加 `> ⚠️ 矛盾提醒：...` 引用对方
- **断链修复**：`[[链接]]` 指向不存在的文件时，要么创建、要么改为纯文本
- **不存代码细节**：Git 历史、代码结构、bug 修复细节不存（代码仓库本身就有）
- **只存非显而易见的**：决策理由、踩坑经验、配置原因、用户偏好
