---
name: ""
metadata: 
  node_type: memory
  tags: 
    - 入口
  originSessionId: 53099e30-6fac-4ddc-b8d1-0dd8bee1197a
  modified: 2026-08-07T06:12:01.619Z
---

# 第二大脑 · AI 操作手册

> 版本 v2.0 | 2026-08-07

## 这是什么

这是一个 **Obsidian 知识库**（也叫"第二大脑"或"记忆系统"），用来持久化保存跨会话的知识。
你是维护这个知识库的 AI 助手，下面是你需要遵守的规则。

## 目录结构（按人看得懂的方式分类）

```
memory/
├── MEMORY.md          ← 总索引（每次会话自动加载）
├── CLAUDE.md          ← 本文件（AI 操作手册）
│
├── 🎮 游戏/           ← 玩的
│   ├── 在用/          ←   还在玩的
│   └── 废弃/          ←   不玩了的
│
├── 🔧 工具/           ← 干活用的
│   ├── 在用/          ←   还在用的
│   └── 废弃/          ←   不用了的
│
├── 🧠 知识/           ← 记录、思考、规则、参考
├── 🤖 自动化/         ← AI 自己跑的
├── 📰 日报/           ← 每日自动生成的新闻和巡检
├── 📦 存档/           ← 每日进度流水账 + 历史基线
│
└── .obsidian/         ← Obsidian 配置（不要改）
```

## 文件命名

- **所有 .md 文件用中文命名**（Obsidian 图谱直接显示文件名）
- 存档文件用日期格式：`YYYY-MM-DD-进度.md`
- 项目文件用项目中文名：`中国象棋.md`、`手机Claude.md`

## Frontmatter 规范

每个记忆文件开头必须有 YAML frontmatter，包含：

```yaml
---
name: <短横线连接的英文slug>
description: <一句话描述，用于 AI 判断相关性>
tags: [标签1, 标签2]            # 可选，Obsidian 图谱用，必须放顶层！
metadata:
  type: user | feedback | project | reference  # 必填
  modified: <ISO 8601 时间戳>                   # 每次修改必须更新
---
```

> ⚠️ **tags 必须放 frontmatter 顶层**（和 name/description 同级），不能嵌套在 metadata 里面。Obsidian 只认顶层 tags，嵌套的不生效。

项目类文件额外包含：
```yaml
metadata:
  type: project
  status: 活跃 | 稳定 | 待修 | 已放弃 | 运行中 | ...（可自定义，优先具体描述）
  version: vX.Y（如果有）
```

## 链接格式

- 使用 Obsidian wikilink：`[[路径/文件名]]`（不带 .md 后缀）
- 链接不存在 → 视为"待创建"，可以后续补
- MEMORY.md 中的每个条目格式：`- [标题](文件.md) — 一句话说明`

## 你的职责

### Obsidian 图谱颜色约定（严格遵守）

| 颜色 | 匹配规则 | 目录 | 含义 |
|------|----------|------|------|
| 🟣 紫 | `tag:#入口` | 根目录 | 入口文件（CLAUDE.md、MEMORY.md） |
| 🔘 灰 | `tag:#已放弃` | 废弃/ | 不用的（覆盖任何目录色） |
| 🟢 绿 | `path:游戏/在用` | 游戏/在用/ | 在玩的游戏 |
| 🔵 蓝 | `path:工具/在用` | 工具/在用/ | 在用的工具 |
| 🩵 青 | `path:知识/` | 知识/ | 记录、思考、规则 |
| 🟠 橙 | `path:自动化/` | 自动化/ | AI 自己跑的 |
| 🩷 粉 | `path:日报/` | 日报/ | 每日新闻和巡检 |
| 🟡 金 | `path:存档/` | 存档/ | 历史存档 |

**铁律：**
- 每个目录都必须有颜色。新增目录时 → 同步更新 `graph.json` 和上表。
- 任何文件都不能是黑色。
- 分类按**人看得懂**的维度：这是干嘛的 → 还能不能用。

### 每次会话结束时（自动）
1. 更新 `存档/当天日期-进度.md`：追加今日完成的事项
2. 更新涉及到的对应记忆文件：版本号、改动摘要、待办
   - 游戏项目 → `游戏/在用/xxx.md` 或 `游戏/废弃/xxx.md`
   - 工具 → `工具/在用/xxx.md` 或 `工具/废弃/xxx.md`
   - 知识 → `知识/xxx.md`
   - 自动化 → `自动化/xxx.md`
3. `git add -A && git commit -m "..." && git push`

### 每周一次（建议周日晚上）
4. 运行 `/记忆体检` — 检查知识库健康度
5. 运行 `/记忆周报` — 生成本周总结

### 用户说"保存进度"时
6. 立即执行上述 1-3 步，不要等会话结束

## 判断属于哪个项目

按优先级：
1. **文件路径**：改了哪个目录下的文件
2. **对话关键词**：用户提到的项目名
3. **纯分析讨论**：根据对话主题匹配
4. **判断不了就问用户**

## 维护原则

- **新增前先查重**：检查是否已有类似文件，更新旧文件而非创建新的
- **过时标记**：信息过时了不要删，在 frontmatter 加 `status: archived`，正文保留
- **矛盾标记**：发现两个文件说法矛盾，在两者正文末尾加 `> ⚠️ 矛盾提醒：...` 引用对方
- **断链修复**：`[[链接]]` 指向不存在的文件时，要么创建、要么改为纯文本
- **不存代码细节**：Git 历史、代码结构、bug 修复细节不存（代码仓库本身就有）
- **只存非显而易见的**：决策理由、踩坑经验、配置原因、用户偏好
