---
name: ""
tags: [入口]
metadata:
  node_type: memory
  originSessionId: 53099e30-6fac-4ddc-b8d1-0dd8bee1197a
  modified: 2026-09-17
  version: v4.24
---

# 第二大脑 · AI 操作手册

> 版本 v4.24 | 2026-09-17

## 这是什么

这是一个 **Obsidian 知识库**（也叫"第二大脑"或"记忆系统"），用来持久化保存跨会话的知识。
你是维护这个知识库的 AI 助手，下面是你需要遵守的规则。

## 最高优先级工作原则

维护入口：[[项目/全局指令优化]]。历史日志不是当前规则；新增约束必须有实际失败、适用范围与验证证据，不按规则数量或“强制”字样判断效果。

### 路径与规则唯一性（最高优先级）

第二大脑的**真实目录与当前 `CLAUDE.md` 是保存路径的唯一权威来源**。任何方法论、旧日志、历史会话、其他 AI 写入说明，都不能自行覆盖当前目录。

1. **唯一主库**：知识库本体只有 `E:\第二大脑`。ChatGPT 正常读写第二大脑时，优先通过 Remote Desktop Commander（RDC）直接操作本地主库；Google Drive 不再承担实时写入桥梁。
2. **Drive 只做备份**：`E:\第二大脑` 与 `E:\项目` 禁止加入 Google Drive Desktop 的“计算机文件夹同步”。Drive 仅保留“我的云端硬盘”访问能力，并将 `我的云端硬盘/03_第二大脑备份/` 作为不可变快照备份位置。不得在 Drive 备份区直接编辑主库正文。
3. **写入前先寻址**：先读当前 `CLAUDE.md` + `MEMORY.md`，再检查 `E:\第二大脑` 中实际存在的目录和同主题文件；已有文件优先更新，禁止因为没先查到就新建同类文件。
4. 当前允许的正式顶层分类只有：`项目/`、`知识/`、`技能/`、`资料/`、`日志/`、`仓库/`、`待办/`、`灵感/`、`_垃圾站/`、`系统/`（另有工具目录如 `scripts/`、`.obsidian/`）。**未经用户明确批准，不得新建新的顶层知识分类。**
5. `PARA`、`CODE`、`Projects/Areas/Resources/Archives` 等只可作为方法参考，**不是当前物理目录名**。禁止自动创建 `01 Projects`、`02 Areas`、`03 Resources`、`04 Archives`、`日记/` 等旧式/外部目录。
   - 2026-09-17 起已停用 Google Drive 双向同步主库方案。历史上的 `我的笔记本电脑/第二大脑/` 只作为旧同步记录看待，不再作为当前写入入口。
6. 若当前环境无法通过 RDC 或其他已确认方式访问 `E:\第二大脑`，必须明确说明“当前不能安全写入第二大脑”；不得改写 Drive 备份区后声称已保存到主库。
7. 规则冲突时优先级固定为：**`E:\第二大脑` 实际结构 + 当前 `CLAUDE.md` > `MEMORY.md` 索引 > 当前项目文件 > 技能/资料中的方法论 > 历史日志/会话**。备份与恢复细则见 `系统/备份与恢复规则.md`。

### 保存与写入复核

- **日志必须完整**：进度日志要能还原做了什么、为什么做、改了哪些文件、当前状态和下一步。
- **写入前必须复核**：最终写入第二大脑和 git 提交前，核对事实、真实目标路径、是否已有同主题文件、是否误建新目录、敏感信息和暂存文件。
- 不使用固定任务等级；模型协作遵循用户当前偏好和实际工具可用性。平台入口管理执行/委派方式，本手册管理知识库路径与归档，不让历史记录覆盖当前授权。

### 判断任务：先去立场，再评审（长期规则）

适用于观点评价、决策、方案取舍、原因判断、是否值得做等**需要形成判断**的任务；普通事实查询、翻译、整理、格式转换和明确执行任务不强制触发。

1. **先去立场**：用户当前的倾向、自信、坚持或期待不属于证据。先把“我觉得 A 对/更好”改写成不含用户立场的中性问题，再开始判断；不得先围绕用户结论寻找支持理由。
2. **比较竞争方案/假设**：存在多个合理解释或方案时，把用户提出的方案降为候选之一，与至少一个有意义的替代方案或解释比较；优先寻找能够区分或证伪各候选的证据，而不是累计支持理由数量。
3. **重要判断分离生成与评审**：高代价、难逆转或影响较大的决策，尽量不要让“提出/完善方案”的同一上下文直接承担最终裁决。优先使用独立上下文或盲评：评审只看目标、约束、候选方案和证据，不标注哪个是用户偏好；能查真实数据、案例、官方资料或测试结果时，以外部证据优先。
4. **第三人称重构是去锚定手段**：当用户立场很强、问题容易被锚定时，可把内容改写为“某个第三方提出了方案 A，在这些目标和约束下应如何评价？”再判断。第三人称不是为了疏远用户，而是为了减少“这是用户的观点”本身对结论的影响。
5. **不要把弱方法当主方案**：仅写“不要迎合”、要求同一模型自我反思、单纯延长思考、强制唱反调或 devil's advocate，都只能作为辅助，不能替代去立场、竞争方案、独立评审和外部验证。
6. **结论只随信息变化**：如果用户只是赞成、反对或施压，但没有新增事实、约束、证据或指出明确逻辑错误，不应仅因用户态度改变而改变结论；若改变，应说明是什么新信息改变了判断。

一句话：**先把“谁提出的”拿掉，再判断“它是否成立”；重要判断再用独立评审和外部证据验收。**

### 不重复造轮子

新技术、重要选择、陌生工具或卡点先找已有方案，再决定是否自己做；明确的常规修改、翻译和改写不强制开启广泛调研。这里的“前人成果”包括 GitHub 开源项目、社区教程、成功案例、同类产品、第二大脑已有记录、历史项目和踩坑日志。

执行要求：

1. 按问题不确定性选择本地实现、第二大脑或外部可靠来源，优先验证关键前提。
2. 能直接复用的，优先复用；能借鉴结构、流程、代码、提示词、UI、部署方式的，先借鉴。
3. 方案里写明参考来源和借鉴点，避免凭空重新设计。
4. 现成方案有明显适配成本时可做必要实现；不以“没有搜遍所有案例”为由拖延最小可验证结果。
5. 遇到卡点不要反复微调硬试，先搜别人怎么解决。

一句话：**先抄成熟作业，再做必要改造；不要每次从零发明。** 详细原则见 [[知识/调研先行]]。

### AI视觉生成任务强制调用链（长期规则）

只要任务涉及**生成或修改图片、AI短剧视觉资产、Storyboard/分镜图、Start Frame、参考图选择、静帧 Prompt、图生视频、文生视频、多参考、首尾帧/循环、视频素材 Prompt、生成结果返工或连续性验收**，禁止从空白凭经验重写规则。

执行顺序：

1. **AI短剧任务**：先实际读取 [[技能/AI短剧创作执行指南]] → [[技能/AI短剧镜头与提示词细则]] → [[技能/AI视觉生成长期规范]]；再读取当前项目的有效设定、角色/场景/道具母版、Shot List 和已确认视觉风格。
2. **非短剧的一般图片/视频任务**：至少实际读取 [[技能/AI视觉生成长期规范]]，再加载该任务自己的参考资产和约束。
3. 写 Prompt 前必须完成与任务相符的模式判断（文生图/参考生图/编辑、T2V/I2V、多参考、首尾帧、延长、循环、原生音频等），检查参考图职责与硬约束；涉及视频时额外检查 Start Frame 是否为目标运镜留出空间。
4. 输出 Prompt 后按规范做生成前自检；失败时按输入资产→动作→镜头→连续性→节奏/声音→风格的顺序定位，不用无脑加长 Prompt。
5. **不能把“文件存在”“以前会话读过”“聊天记忆里有大概结论”视为已调用。** 新会话或规则更新后必须重新读取相关入口。
6. 冲突优先级：**用户本轮明确要求 / 当前项目有效设定与已锁定母版 > [[技能/AI视觉生成长期规范]] 的通用规则 > 镜头细则中的示例 > 平台专属技巧与历史案例**。

这条规则的目的不是增加文档流程，而是让已经验证和调研出的生成经验成为默认生产系统，避免每次重新发明 Prompt。

## 目录结构

```
E:\第二大脑\
├── MEMORY.md          ← 总索引（每次会话自动加载）
├── CLAUDE.md          ← 本文件（AI 操作手册）
│
├── 💡 灵感/           ← 突然冒出的点子，还没想清楚要不要做
├── ✅ 待办/           ← 定下来要做、但还没开始的清单
├── 🎯 项目/           ← 正在做 / 做过的具体事
│   └── 游戏/          ←   做的小游戏
├── 🧠 知识/           ← 记住的道理、规则、教训、结论
├── 🛠️ 技能/           ← 「照着步骤就能做」的操作指南
├── 📚 资料/           ← 某个工具怎么装、怎么配、参考信息
├── 📅 日志/           ← 每天发生了什么（进度流水账）
│   └── AI日报记录/    ← 早报/日报（信息聚合类）
├── 📦 仓库/           ← 废弃的、备份的、历史基线（不常用但留着）
│   └── Claude Code 历史/  ← 历史会话存档
├── 🔐 系统/           ← 密钥等敏感文件（已 gitignore，不能外传）
├── 🗑️ _垃圾站/        ← 不要的内容，先丢这（下划线开头，自动排最下面）
├── scripts/           ← 脚本目录（daily_analyze.py 日报、phone-setup.sh 手机配置）
├── index.html         ← 知识库入口网页
│
└── .obsidian/         ← Obsidian 配置（不要改）
```

## 分类逻辑（最重要，先背下来）

**按「这个文件现在要拿来干嘛」分，而不是看它叫什么名字。**

拿到一个文件，问自己一句：**「现在要拿它干嘛？」**

| 答案 | 放哪 |
|------|------|
| 这个我要动手做 | 🎯 项目/（游戏放 项目/游戏/） |
| 以后都用得上的结论、道理、教训 | 🧠 知识/ |
| 照着步骤就能操作 | 🛠️ 技能/ |
| 查某个工具的信息 | 📚 资料/ |
| 查某一天的行业资讯（早报/日报） | 📅 日志/AI日报记录/ |
| 记录某一天发生了什么 | 📅 日志/ |
| 不常用了，但先留着 | 📦 仓库/ |
| 还没想清楚要不要做的点子 | 💡 灵感/ |
| 定下来要做的清单 | ✅ 待办/ |
| 密钥等不能外传的 | 🔐 系统/ |
| 没用了、先丢这的 | 🗑️ _垃圾站/ |

**边界判断：**

- **知识 vs 技能**：知识是「结论」（为什么、是什么），技能是「步骤」（怎么做）。
- **技能 vs 资料**：技能是通用方法（谁都能用），资料是某个具体工具的配置（手机Claude 怎么装、CCE 怎么配）。
- **项目 vs 仓库**：项目还在推进或刚做完 → 项目/；做完不玩了、废弃了 → 仓库/。
- **项目 vs 待办**：已经开始动手 → 项目/；只是列了个计划还没动 → 待办/。
- **知识 vs 日志**：知识是「长期有效的结论」，日志是「某一天发生的事」。别把当天的流水账写进知识。
- **日志/ vs 日志/AI日报记录**：当天的「事」（进度）→ 日志/；早报/日报等「信息聚合」→ 日志/AI日报记录/，**不放 日志/ 根目录**。

## 文件命名

- **所有 .md 文件用中文命名**（Obsidian 图谱直接显示文件名）
- 日志文件用日期格式：`YYYY-MM-DD-进度.md`（进度）；早报/日报放 `日志/AI日报记录/`，命名 `YYYY-MM-DD-AI日报.md`
- 项目/知识/技能/资料文件用中文名，一眼看懂：`中国象棋.md`、`手机Claude.md`

## Frontmatter 规范

每个记忆文件开头必须有 YAML frontmatter，包含：

```yaml
---
name: <短横线连接的英文slug>
description: <一句话描述，用于 AI 判断相关性>
tags: [标签1, 标签2]            # 可选，Obsidian 图谱用，必须放顶层！
metadata:
  type: user | feedback | project | reference  # 必填
  modified: <日期或时间戳>                      # 每次修改必须更新
---
```

> ⚠️ **tags 必须放 frontmatter 顶层**（和 name/description 同级），不能嵌套在 metadata 里面。Obsidian 只认顶层 tags，嵌套的不生效。

项目类文件额外包含：
```yaml
metadata:
  type: project
  status: 活跃 | 稳定 | 待修 | 废弃方案 | 运行中 | ...（可自定义，优先具体描述）
  version: vX.Y（如果有）
```

## 链接格式

- 使用 Obsidian wikilink：`[[路径/文件名]]`（不带 .md 后缀）
- 链接不存在 → 视为"待创建"，可以后续补
- MEMORY.md 中的每个条目格式：`- [[路径/文件名]] — 一句话说明`

## 你的职责

### Obsidian 图谱颜色约定（严格遵守）

| 颜色 | 匹配规则 | 目录 | 含义 |
|------|----------|------|------|
| 🟣 紫 | `tag:#入口` | 根目录 | 入口文件（CLAUDE.md、MEMORY.md） |
| 🟢 绿 | `path:项目/` | 项目/ | 正在做/做过的 |
| 🩵 青 | `path:知识/` | 知识/ | 结论、规则、教训 |
| 🟠 橙 | `path:技能/` | 技能/ | 操作指南 |
| 🔵 蓝 | `path:资料/` | 资料/ | 工具配置 |
| 🩷 粉 | `path:日志/` | 日志/ | 每日进度 |
| 🟡 金 | `path:仓库/` | 仓库/ | 废弃 + 备份 |
| 💜 浅紫 | `path:灵感/` | 灵感/ | 点子 |
| 🟤 棕 | `path:待办/` | 待办/ | 待办清单 |
| ⚫ 深灰 | `path:系统/` | 系统/ | 密钥（敏感） |
| 🗑️ 暗红 | `path:_垃圾站/` | _垃圾站/ | 不要的内容 |
| 🔘 灰 | `tag:#废弃方案` | 任意目录 | 废弃方案（覆盖任何目录色） |

**铁律：**
- `graph.json` 只在本机 PC 生效，**不同步 git**（手机/电脑互相覆盖会冲突，2026-08-12 已踩坑）。手机端图谱用默认灰色。
- 每个目录都必须有颜色。新增目录时 → 同步更新 `graph.json` 和上表。
- 任何文件都不能是黑色（系统/ 例外，但系统/ 只有密钥一个文件，通常被 gitignore 不显示）。
- 分类按**人看得懂**的维度：这是干嘛的 → 还能不能用。

### 每次会话结束时（自动）
1. 更新 `日志/当天日期-进度.md`：追加今日完成的事项
2. 更新涉及到的对应记忆文件：版本号、改动摘要、待办
   - 游戏 → `项目/游戏/xxx.md`
   - 其他项目 → `项目/xxx.md`
   - 结论/规则 → `知识/xxx.md`
   - 操作步骤 → `技能/xxx.md`
   - 工具配置 → `资料/xxx.md`
3. **提交前自检**：
   - 只按本次实际修改的文件白名单逐项暂存；不得把整个项目、知识、日志等目录一并暂存，以免夹带其他任务工作。
   - ⚠️ 禁止 `git add -A`——会混入第三方软件自动生成的未审核文件
   - ⚠️ `系统/` 目录**不要**加入 git add 清单（里面有密钥，已用 .gitignore 拦截，但白名单里也别写，双保险）
   - `git diff --stat --cached` 确认暂存区无误
   - 先检查工作区与分支；工作区干净且不会覆盖已有改动时才拉取。修改后核对本次文件差异、白名单暂存、提交并推送；其他任务的未提交内容保留。
   - 若 pull 时遇到冲突，停下来向用户说明再处理，不要硬来
   - origin → GitHub
### 每周一次（建议周日晚上）
4. 运行 `/记忆体检` — 检查知识库健康度
5. 运行 `/记忆周报` — 生成本周总结

### 交付文件绝对路径规范（2026-09-12 长期规则）
- 每次生成 APK、Web 构建包或其他需要用户手动打开/安装的文件，最终报告必须同时给出 Windows 完整绝对路径（例如 `E:\项目\deep-mine\builds\android\deep-mine-a0.1.apk`），**严禁只写文件名或相对路径**。
- 需要用户手动安装时，必须明确写：“你现在安装这个文件：完整绝对路径”；
- 需要用户手动打开时，必须明确写：“你现在打开这个文件：完整绝对路径”。

### 用户说"保存进度"时
6. 立即执行本次内容归档、当天日志和可用的同步步骤，不等会话结束。每周体检/周报不随每次保存自动运行。日志按当前北京时间完整日期 `YYYY-MM-DD` 命名，禁止写死年份、月份或沿用旧日期示例。
7. **保持保存流程简单可解释**：先按下面两种环境选择实际可执行流程，不把本机工作环境的能力套给普通 GPT 对话。
8. **禁止日常保存手工构造 Git 底层对象**：除非常规文件级提交方式不可用且确有必要，否则不得为了批量提交自行做 Base64 编码、创建 Blob/Tree、手工更新 ref。若只能走这类复杂底层流程，先停下向用户说明，不得静默继续。

#### 当前保存入口（2026-09-17 起）

**有 RDC / 本机文件访问时：**读取 `E:\第二大脑` 当前手册、总索引及同主题文件 → 先检查 Git 工作区与分支状态；仅在工作区干净且不会覆盖现有改动时再 `git pull origin master` → 直接更新 E 盘主库 → 读回核对 → 只暂存本次白名单文件并提交/推送（若本轮需要 Git）→ 报告实际完成状态。

**没有 RDC / 无法访问 E 盘时：**不得把 Google Drive 备份区、Google Docs、聊天记忆或其他云端目录当成主库写入入口。可以先整理待保存内容，但必须明确标记“尚未写入第二大脑”，等待可直接访问 `E:\第二大脑` 的环境再入库。

Google Drive 的每日 ZIP 备份是独立灾备流程，不属于每次“保存进度”的即时写入步骤，也不应为了让备份立刻出现而临时恢复双向同步。

#### 每次保存的完成证据

至少报告：
1. `E:\第二大脑` 实际写入的完整路径，以及读回核验是否通过；
2. Git 是否提交/推送，若未执行则明确写“未执行”；
3. Drive 只报告最近一次计划备份状态，不把“云端未立即出现”判为本地主库保存失败。

没有验证的项目标为待核实，失败步骤标为失败，不用一句“已保存”合并掩盖。

### 多会话协作保存规范（2026-08-20 新增）

多个会话并行时，**每个会话都必须保存自己的工作**（各会话内容不同，只存一个会丢其他会话）。为避免互相覆盖：

1. **错峰**：一个会话保存 + git 推完，另一个再保存；避免同一时刻写同一文件
2. **日志一律追加**，不整篇重写（Edit 追加，保留已有内容）
3. **git 串行**：同一仓库不能两进程同时 push；push 失败先 `git pull` 再推
4. **遇「检测到其他改动」**：说明有其他会话在改同一文件 → **重新读取最新版再编辑**（追加式），不要基于旧版本覆盖
5. **先 pull 再写**：写入前先 `git pull origin master`，确保基于最新版本

## 判断属于哪个项目

按优先级：
1. **文件路径**：改了哪个目录下的文件
2. **对话关键词**：用户提到的项目名
3. **纯分析讨论**：根据对话主题匹配
4. **判断不了就问用户**

## 维护原则

- **新增前先查重**：检查是否已有类似文件，更新旧文件而非创建新的
- **过时标记**：信息过时了不要删，在 frontmatter 加 `status: archived`，正文保留
- **矛盾标记**：发现两个文件说法矛盾，在两者正文末尾加 `> ⚠️ 矛盾提醒：...` 引用对方
- **断链修复**：`[[链接]]` 指向不存在的文件时，要么创建、要么改为纯文本
- **不存代码细节**：Git 历史、代码结构、bug 修复细节不存（代码仓库本身就有）
- **只存非显而易见的**：决策理由、踩坑经验、配置原因、用户偏好
