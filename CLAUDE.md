# 第二大脑 · AI 操作手册

> 版本 v1.3 | 2026-08-07

## 这是什么

这是一个 **Obsidian 知识库**（也叫"第二大脑"或"记忆系统"），用来持久化保存跨会话的知识。
你是维护这个知识库的 AI 助手，下面是你需要遵守的规则。

## 目录结构

```
memory/
├── MEMORY.md          ← 全局索引（每个会话自动加载）
├── CLAUDE.md          ← 本文件（AI 操作手册）
├── 参考/              ← 流程、原则、工具清单等参考文档
├── 项目/              ← 每个项目一个文件，记录版本、状态、待办
├── 存档/              ← 每日进度流水账 + 历史基线
├── 每日早报/          ← 每日自动生成的新闻摘要 + 项目健康巡检
└── .obsidian/         ← Obsidian 配置（不要改）
```

## 文件命名

- **所有 .md 文件用中文命名**（Obsidian 图谱直接显示文件名）
- 存档文件用日期格式：`YYYY-MM-DD-进度.md`
- 项目文件用项目中文名：`中国象棋.md`、`手机Claude.md`

## Frontmatter 规范

每个记忆文件开头必须有 YAML frontmatter，包含：

```yaml
---
name: <短横线连接的英文slug>
description: <一句话描述，用于 AI 判断相关性>
metadata:
  type: user | feedback | project | reference  # 必填
  modified: <ISO 8601 时间戳>                   # 每次修改必须更新
---
```

项目类文件额外包含：
```yaml
metadata:
  type: project
  status: 活跃 | 稳定 | 待修 | 已放弃 | 运行中 | ...（可自定义，优先具体描述）
  version: vX.Y（如果有）
```

## 链接格式

- 使用 Obsidian wikilink：`[[路径/文件名]]`（不带 .md 后缀）
- 链接不存在 → 视为"待创建"，可以后续补
- MEMORY.md 中的每个条目格式：`- [标题](文件.md) — 一句话说明`

## 你的职责

### Obsidian 图谱颜色约定（严格遵守）

- 🔘 **灰色（rgb 8421504 / #808080）**：**仅限已放弃的项目**。通过 `tags: [已放弃]` 匹配。
- 🟢 绿色（rgb 6084188）：`项目/` 目录，活跃项目自动归入。
- 🔵 蓝绿色（rgb 4886745）：`参考/` 目录。
- 🟡 黄色（rgb 15769680）：`存档/` 目录。
- **新建项目**：创建在 `项目/` 下 → 自动显示为绿色。**绝不**给新项目加 `tags: [已放弃]` 或灰色。
- **放弃项目时**：① 改 frontmatter `status: 已放弃` ② 加 `tags: [已放弃]` ③ 图谱自动变灰。

### 每次会话结束时（自动）
1. 更新 `存档/当天日期-进度.md`：追加今日完成的事项
2. 更新涉及到的 `项目/xxx.md`：版本号、改动摘要、待办
3. `git add -A && git commit -m "..." && git push`

### 每周一次（建议周日晚上）
4. 运行 `/记忆体检` — 检查知识库健康度
5. 运行 `/记忆周报` — 生成本周总结

### 用户说"保存进度"时
6. 立即执行上述 1-3 步，不要等会话结束

## 判断属于哪个项目

按优先级：
1. **文件路径**：改了哪个目录下的文件
2. **对话关键词**：用户提到的项目名
3. **纯分析讨论**：根据对话主题匹配
4. **判断不了就问用户**

## 维护原则

- **新增前先查重**：检查是否已有类似文件，更新旧文件而非创建新的
- **过时标记**：信息过时了不要删，在 frontmatter 加 `status: archived`，正文保留
- **矛盾标记**：发现两个文件说法矛盾，在两者正文末尾加 `> ⚠️ 矛盾提醒：...` 引用对方
- **断链修复**：`[[链接]]` 指向不存在的文件时，要么创建、要么改为纯文本
- **不存代码细节**：Git 历史、代码结构、bug 修复细节不存（代码仓库本身就有）
- **只存非显而易见的**：决策理由、踩坑经验、配置原因、用户偏好
