---
name: 2026-08-16
description: 2026-08-16 会话 — 用户语言风格画像分析 + 提示词优化清单，落地 Obsidian
metadata: 
  node_type: memory
  type: project
  modified: 2026-08-16
  originSessionId: current
---

# 2026-08-16 进度

## 🎨 用户语言风格画像 + 提示词优化清单

用户要求：只调研我们俩的历史对话（206 段 / 158MB），只分析用户说的话，提炼语言风格，做两件事——①整理语言风格（正面、可复用、让 AI 了解用户）；②找出拖累提示词质量的优化点做成对比清单。

### 数据（客观统计）

- **1605 条**人类消息，**18.6 万字**，时间跨度 2026-07-25 → 2026-08-16
- 平均每条 116 字（被大段粘贴代码拉高）；≤10 字的消息 431 条（命令式）
- 语气词：吗 138、吧 60、呢 46、啊 37
- 口头禅：就 455、这个 254、就是 186、然后 123、而且 86
- 30 秒内连发 243 次（习惯一句话拆几条发、逐条补充）
- 语音输入同音错字高频（保存进度→「保持距离」、微信→「温馨」、需要→「树妖」…）

### 产出（已落地 Obsidian）

| 文件 | 版本 | 说明 |
|------|------|------|
| 知识/用户语言风格.md | v1.0 | 正面画像：说话特征、高频词真实含义、思维习惯、错字对照、AI 该怎么回 |
| 项目/提示词优化清单.md | v1.0 | 8 个优化点对比清单（每个配反例+正例） |
| 知识/用户人物画像.md | v1.2 | 「九、相关记忆」加链接，v1.1 → v1.2 |
| MEMORY.md | — | 索引补 2 行（知识区+项目区） |

### 关键结论

- 用户语言风格 = 「微信语音转文字的说话方式，直接用在给 AI 下指令上」
- 最该先改的 4 个优化点：① 把「这个/那个」换具体名字 ③ 一句话只说一件事 ④ 说目的别说手段 ⑤ 补对象+目标+验收标准

### 待办

- 用户可能后续要：把优化清单做成「可直接套用的话术模板」

## 改动的文件

- `E:\第二大脑\知识\用户语言风格.md`（新建 v1.0）
- `E:\第二大脑\项目\提示词优化清单.md`（新建 v1.0）
- `E:\第二大脑\知识\用户人物画像.md`（加链接 + 升 v1.2）
- `E:\第二大脑\MEMORY.md`（索引补 2 行）
- `E:\第二大脑\存档\2026-08-16-进度.md`（本文件）

## 🔗 读链接 + 识图能力（Claude Code 扩展）

给 Claude Code 加两个能力：识图（mcp-vision + 智谱 GLM-4V-Flash）和读链接（Skill + yt-dlp + 智谱 GLM-ASR）。

### 结果

- 识图 ✅ 已装并验证（GLM-4V-Flash status 200）
- 读链接 ✅ 网页/音频/视频转写链路全通；抖音/B站需浏览器登录 cookie（实测 B 站无登录 412）
- 关键决策：ASR 弃本地 FunASR（torch/onnxruntime DLL 加载失败 ×3，workbuddy Python 3.13 环境问题），改智谱 GLM-ASR 纯 HTTP API

### 改动文件

- 新建 `C:\Users\Administrator\.claude\skills\读链接\`（SKILL.md v1.1 + scripts/read_link.py）
- `claude mcp add` 写入 mcp-vision 配置
- 项目文件《Claude Code 环境维护.md》升 v1.4

## 🔑 API 密钥存放（GPT key）

用户给了 OpenAI GPT API key（sk-proj-...），要求存入记忆库备用（怕以后找不到）。

### 结果

- 新建 `E:\第二大脑\工具\API密钥.md`（v1.0）——集中存放所有 API key：GPT key 明文 + DeepSeek key 位置
- 关键决策：该文件加入 `.gitignore`，明文 key 不进 GitHub（沿用之前 DeepSeek key 存 GitHub Secrets、笔记只记位置的原则）
- MEMORY.md 工具区补 1 行索引；`git check-ignore` 已验证密钥文件被忽略 ✅

### 改动文件

- `E:\第二大脑\工具\API密钥.md`（新建 v1.0，已 gitignore）
- `E:\第二大脑\.gitignore`（+1 行忽略规则）
- `E:\第二大脑\MEMORY.md`（工具区 +1 行索引）
- `E:\第二大脑\存档\2026-08-16-进度.md`（本文件）

## 🚀 读链接 v1.3 升级：抖音免登录 + 本地 FunASR（免费）

延续上面读链接任务，攻克两个关键难点（用户实测的抖音链接已跑通）。

### 突破 1：本地 FunASR 免费转写（弃智谱 GLM-ASR 计费）

- 根因推翻：之前「FunASR 装不起来」的真实原因是缺 VC++ 运行库 `vcruntime140_2.dll`（不是 Python 3.13 的问题）。winget 装 `Microsoft.VCRedist.2015+.x64` + 装标准 Python 3.11 后，torch + funasr 全部 import 成功。
- 验证：SenseVoiceSmall 模型转写「今天天气很好，我们一起去公园玩」完全正确，rtf 0.14（比实时快 7 倍）。
- 效果：语音转写彻底免费无限量，不再用智谱 ASR。

### 突破 2：抖音免登录下载（攻克 a_bogus 签名墙）

- 之前卡在「Fresh cookies needed」。按「借鉴别人路径」要求，SSH clone 开源方案 `Douyin_TikTok_Download_API` + `abogus_cpp` 看源码，打通 a_bogus 签名。
- 原理：短链 → aweme_id → 游客 ttwid → a_bogus 签名（gmssl SM3）→ detail API → 视频直链。
- 验证：用户实测链接 `v.douyin.com/xn8UvyQCqRE/` 端到端跑通，拿到标题「零基础搭AI知识库…」+ 作者「姜胡说」+ 转写全文。

### 版本

- SKILL.md v1.3、项目文件《Claude Code 环境维护.md》v1.5
- 新增 `scripts/douyin_sign.py`（a_bogus 签名模块，复制自开源）
- read_link.py 改用 Python 3.11 运行（装 torch/funasr 的标准环境）
