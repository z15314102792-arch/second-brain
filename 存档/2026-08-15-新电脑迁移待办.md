---
name: ""
description: 旧电脑留下的待办：新电脑 Claude Code 接手完成 AI 环境迁移（重装软件+恢复数据+项目迁D盘）
metadata: 
  node_type: memory
  type: project
  status: 已完成
  version: v1.1
  originSessionId: 5db7b9a7-c3ac-4d2c-ba43-a7b24a89aeae
  modified: 2026-08-15T06:08:52.938Z
---

# 新电脑迁移待办

> ⚠️ **这是给新电脑上的 Claude Code 读的。** 如果你正在新电脑读到这里，请接手完成下面的清单。用户是小白，每一步都要通俗解释。

## 背景

用户从旧电脑迁移到新电脑（2026-08-15）。所有数据已搬到移动硬盘的 `ClaudeCode迁移\` 文件夹，结构如下：

```
ClaudeCode迁移\
├── .claude\           ← 记忆库+配置+API密钥（256M）
├── .claude.json       ← 全局状态
├── projects\          ← 9个项目（游戏+工具）
├── ai-tools\          ← AI工具数据（.cce多模型配置、.ollama模型7.5G、.workbuddy等）
├── restore-claude.bat ← 一键恢复脚本
└── 使用说明.txt
```

## 待办清单

### ① 确认数据已恢复
- [ ] 让用户确认运行过 `restore-claude.bat`（在移动硬盘 `ClaudeCode迁移\` 里）
- [ ] 检查 `C:\Users\Administrator\.claude` 存在且含 memory
- [ ] 检查 `.cce`（多模型配置）已恢复到用户目录

### ② 项目迁到 D 盘（用户明确要求，不占 C 盘）
- [ ] 项目应在 `D:\项目\` 下（不是 C 盘根目录）
- [ ] 把本记忆库里所有项目路径从 `C:\项目名` 改成 `D:\项目\项目名`
- [ ] 涉及文件：`游戏/*.md` 的 frontmatter `path` 字段 + 正文路径引用

### ③ 重装软件（数据已搬，只装程序本体）
- [ ] Git：`https://git-scm.com/download/win` 下载安装
- [ ] Obsidian：`https://obsidian.md` 下载安装 → 打开 memory 文件夹作为库（`C:\Users\Administrator\.claude\projects\C--\memory`）
- [ ] Ollama：`https://ollama.com/download/windows` 下载安装（模型已在 `.ollama`，装完即用）
- [ ] 可选 Cursor（cursor.com）、WorkBuddy、CodeBuddy —— 问用户要不要再装
- [ ] 可选 ZeroTier（手机远程，网络ID `154a350c866d74d3`）+ claude-code-remote（npm，装完要改2个文件，见 [[知识/已安装工具]]）

### ④ 验证
- [ ] `claude` 能启动、连上 DeepSeek（settings.json 里 ANTHROPIC_BASE_URL 指向 api.deepseek.com）
- [ ] 多模型切换可用（.cce 的 8 个后端已恢复）
- [ ] 记忆完整（用户问「记得我吗」能答出）
- [ ] 项目能从 D 盘找到

## 相关
- [[知识/已安装工具]] — 软件安装命令参考
- [[工具/CCE模型]] — 多模型配置
- [[工具/WorkBuddy]] — 腾讯AI办公安装避坑
