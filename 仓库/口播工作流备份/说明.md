# 口播自动剪辑工作流 run.ps1 备份

- **版本**：v1.7（2026-08-20 备份）。工作流整体 v1.8，run.ps1 在 v1.8 未改动，v1.7 即当前版。
- **用途**：PowerShell 入口脚本，串联 转录→AI 分析→字幕→渲染→剪映导出→特效音效。
- **编码要求**：UTF-8 **带 BOM**（PowerShell 5.1 无 BOM 按 GBK 读中文会乱码）。

## 丢失历史（重要）
1. **2026-08-19**：主目录 run.ps1 丢失（原因不明），当时从会话记录恢复。
2. **2026-08-20**：再次丢失（原因不明），从会话 jsonl 提取最后一次 Write 内容恢复。

→ **run.ps1 已两次丢失，务必保留本备份**，勿删。

## 恢复方法
若项目目录 run.ps1 再丢：
1. 从本目录 `run.ps1.v1.7.20260820.ps1` 直接复制回项目根目录（已含 UTF-8+BOM）。
2. 或从会话 jsonl 提取：`C:\Users\Administrator\.claude\projects\C--\*.jsonl` 里最后一次对 run.ps1 的 `Write` tool_use 的 `input.content`。

## 部署位置
`C:\Users\Administrator\Documents\Codex\2026-08-17\new-chat\outputs\口播自动剪辑工作流-v1.0\run.ps1`

## 执行注意（PowerShell 中文路径）
- PowerShell 直接 `-File` 中文路径会因 GBK 编码挂掉 → 用 python 驱动复刻调用序列最稳（全 UTF-8）。
- 详见日志 `2026-08-20-进度.md` 技术备忘。
