---
name: 记忆系统
description: 项目记忆系统 — 从单文件流水账重建为结构化多项目记忆体系
metadata: 
  node_type: memory
  type: project
  version: v3.3
  path: E:\第二大脑
  modified: 2026-09-13
  originSessionId: 6dd31fbb-2c41-49f3-808f-f0380c8b1198
---

# 项目记忆系统重建

## v3.3 GPT漏存内容入库与保存规范更正（2026-09-13）

- 用户明确要求将本轮审计候选入库并更正保存规范。补存 [[项目/AI短剧创作学习]]，保留实作目标、免费优先、轻量流程、工具候选需重查、镜头版本和审片15项；不强行锁定时长/工具，不覆盖《失踪者名单》当前阶段。
- [[项目/AI工作流学习]] 升至v0.2，恢复到循环与中间变量练习，明确最新读取消息为9月4日、输出可选项卡点、预期正反用例和未执行的计数练习；不冒称本轮线上验收。
- [[项目/战狼口播素材整理]] 升至v5.1，补存标点、低频原话接续、操盘手引导语处理和逐段时间码细则；较新的内容完整度要求优先，不恢复旧的较低门槛。
- 用户原始1.txt全文转入 [[项目/AI短剧_失踪者名单/11 研究参考/池恹原始人设素材]]，原始22,868字节逐字节保留，摘要与原文用途明确区分，不把原始关系剧情写成有效设定；作品导航新增两项入口。
- CLAUDE.md v4.18更正本机/普通GPT对话保存分支、唯一镜像原始Markdown写入及读回、E盘/Drive/GitHub三项证据、动态日期和每周体检范围；MEMORY.md v4.45同步入口，资料/第二大脑.md v4.14补充说明。
- 本机保存进度技能 `C:\Users\Administrator\.agents\skills\source-command\SKILL.md` v1.1纠正写死8月的路径，并依当前手册区分环境。该技能不在Drive镜像内，普通GPT对话以云端CLAUDE.md/MEMORY.md为规则入口，不假定自动加载本机技能。
- 两份本机全局规则 `C:\Users\Administrator\.codex\AGENTS.md`、`C:\Users\Administrator\AGENTS.md` 均升至v2.5，只更正保存路径、日期和环境分支；主库AGENTS.md已核对，原有日志/和当前手册入口正确，无需改动。
- 此次不修改系统/，不清库、不另建顶层分类，也不自动继续被冻结的深井项目；历史审计候选记录保留为历史，当前状态以本节入库成果为准。

## v3.2 最近 GPT 对话保存检查与普通对话保存边界（2026-09-13）

- 本次读取列表可见的 18 个 GPT 对话，补读短剧、标题、备份、保存位置四个对话至最后一页；共读取 105 轮。游戏与 AI 工作流长对话仍只读取最新 10 轮，不能称为所有聊天全量审计；归档、删除、列表范围外内容及多数附件未审计。
- 已核对主库正文：标题定稿与偏好、深井叙事及暂停/冻结、战狼素材整理主要规则、Drive 唯一入口与清理决定已保存。此次不宣称历史绝对零遗漏。
- 建议补存候选：AI 短剧的低成本实作学习目标、先固定人物/场景与图片再生成视频、镜头审片与失败分析流程；AI 工作流项目的实际学习进度（项目笔记仍停留 8 月 19 日等待注册，聊天已到循环节点/中间变量练习，读取到的消息为 9 月 4 日，非本周新决定）。原始池恹人设附件 1.txt 为 22,868 字节，已能读取，主库已有核心摘要，但未确认原文全文已归档，可选择按素材保留，不能写成当前有效设定。口播主要规则已存在，仅标点处理、低频接续及操盘手提示改写等细节可选择补充到现有项目。
- 本轮仅记录审计结论与待补存建议，不把全部历史聊天或 AI 未确认提案自动写成用户定案。
- 普通 GPT 对话也支持插件（官方 https://learn.chatgpt.com/docs/plugins ），Drive 是否能保存取决于该普通对话实际可用的原始文件上传/内容更新工具和账户权限；不能凭工作模式工具成功就保证普通对话已验证。
- 普通对话正确流程：读唯一镜像当前 CLAUDE.md/MEMORY.md → 找现有主题文件并读取最新版 → 更新正确相对目录中的原始 .md 正文/版本 → 追加日志，必要时更新索引 → 重新读取本次云端文件核对 → 由 Google Drive Desktop 回传 E 盘。若普通对话无法看到 E 盘，只能确认云端写入，必须将本地同步标为待核实；没有原始 Markdown 写入能力时不能改存 Google Docs、Library 或其他 Drive 目录后冒称完成。
- GitHub 版本备份是独立状态：普通对话不能套用本地终端 pull/commit/push；有可用文件级 GitHub 工具时独立执行并核验，否则报告未执行。桌面同步暂停、电脑离线或多人同改时，不能承诺即时同步和永不出错。

**日期**: 2026-08-06
**状态**: ✅ 完成

## 背景

早上发现多个终端 `claude continue` 后全部读到相同内容，无法区分各自进度。根因是 memory 只有一份全局文件，没有"项目身份"概念。

## 做了什么

从 121 个会话文件中提取全部项目信息，把原来的单文件流水账重建为结构化多项目记忆体系。

### 新建文件（10 个 project-*.md）

| 文件 | 项目 | 版本 | 状态 |
|------|------|------|------|
| `中国象棋.md` | 中国象棋 | v3.11 | 稳定 |
| `五子棋.md` | 五子棋 | v1.2 | 正常 |
| `你画我猜.md` | 你画我猜 | v8.13/6.0 | 完备 |
| `星月神殿.md` | 星月神殿 | v2.3 | 关卡待修 |
| `第二大脑.md` | 第二大脑 | - | 活跃 |
| `cc-web.md` | cc-web | - | 已废弃 |
| `CCE模型.md` | CCE 配置 | - | 8模型 |
| `Huppy.md` | Huppy | v1.1.3 | 放弃 |
| `Agnes代理.md` | Agnes 代理 | - | 需手动启动 |
| `免费Claude.md` | NVIDIA 代理 | - | 国内不可用 |

### 新建流程文件

- `找回丢失项目.md` — 丢失项目时从 .jsonl 会话记录中找回的 5 步搜索路径

### 重构文件

- `MEMORY.md` — 三区结构（流程与参考 / 项目 / 存档）
- `会话恢复流程.md` — 添加 找回丢失项目 引用

### CLAUDE.md 变更

- 删除：`找回丢失项目` 自动触发规则（使用频率太低，不应占全局上下文）
- 新增：自动存档判断项目逻辑（文件路径 → 对话关键词 → 纯分析讨论主题 → 询问用户）
- 修复三条：自测豁免 / 版本标示灵活化 / 存档判断盲区

## 关键决策

1. **不按目录隔离**：用户记不住项目路径，与使用习惯冲突
2. **不分级 CLAUDE.md**：没必要，项目身份只需 3 行
3. **不是说"继续象棋"然后我去搜**：用户不需要记路径，他只需要项目名的自然语言
4. **应急流程放 memory，行为准则放 CLAUDE.md**：全局指令筛选原则
5. **改 CLAUDE.md 前自查三问**：能落地吗 / 有更省的吗 / 有副作用吗

## 使用方式

用户打开终端直接打字，不需要记路径：

```
继续象棋    → 搜 中国象棋.md → 恢复
画画        → 搜 你画我猜.md → 恢复
五子棋      → 搜 五子棋.md → 恢复
那个闯关游戏 → 搜 星月神殿.md → 恢复
```

自动存档时，我根据文件路径+对话关键词判断项目，更新对应的 project-*.md。

## 踩过的坑

### 1. Obsidian 置顶文件名
- `_` 前缀**不能**置顶 — `_` ASCII 码 95，排在中文和字母之后
- `!` 前缀**也不能** — 同样排后面
- **结论**：不要折腾特殊字符置顶，就用 `MEMORY.md` 作为入口

### 2. 图谱"全英文"的诊断失误
- 用户说"关系图谱里全是英文文件夹名"，我第一反应是去藏 `.git` 和 `.obsidian` 文件夹
- 实际原因：图谱每个节点是一个 `.md` 文件，文件名直接显示为节点标签。19 个文件全是英文名 → 图谱全英文
- **教训**：用户描述问题时，先搞清楚系统实际显示了什么，不要凭猜测排查。`userIgnoreFilters` 只能隐藏文件，不能改变节点的显示文字

### 3. 文件名应该默认用中文
- Obsidian 和 Git 都完美支持中文文件名
- 英文文件名对中文用户的图谱和文件列表都是障碍
- **规则**：memory 仓库里所有 `.md` 文件用中文命名

## 后续版本改动

### v1.1 (2026-08-06 下午)
- 仓库分类：按 `项目/` `参考/` `日志/` 三级文件夹整理
- 所有文件名中文化：19 个文件从英文改为中文
- 新增 `.obsidian/app.json` `userIgnoreFilters`：隐藏 `.git` `.obsidian` 临时文件
- MEMORY.md 恢复为唯一入口文件（`_入口.md` 和 `!入口.md` 均放弃）

### v1.2 (2026-08-06 晚间)
- 确认 DeepSeek-V4-Pro 上下文窗口为 1M token
- 决策：不设 `modelOverrides`，保持默认 20 万窗口（省钱省时，实际够用）

### v1.5 (2026-08-07)
- 合并两个工作流为 `daily-digest.yml`：AI新闻+健康检查→单条微信推送
- 新增仪表盘 HTML 页面（标签页：今日/往期/关于）
- 仓库公开 + GitHub Pages 启用：https://z15314102792-arch.github.io/second-brain/
- 脱敏：移除明文 API Key

### v2.0 (2026-08-07) — 全面重构：按人看得懂的维度分类
- 旧分类 `项目/` `参考/` `每日早报/` 对非技术用户不直观
- **新结构**：🎮游戏/（废弃/）→ 🔧工具/（废弃/）→ 🧠知识/ → 🤖自动化/ → 📰日报/ → 📦存档/
- 全部 31 个文件 `git mv` 搬迁，所有 wikilink `sed` 批量更新
- MEMORY.md 完全重写为分类索引
- graph.json 8 种颜色覆盖全部目录（每目录必有色，黑色文件零容忍）
- CLAUDE.md v2.0：新目录树 + 颜色表 + 维护铁律
- 废弃项目 tags 修复为 frontmatter 顶层（Obsidian 只认顶层 tags）

### v2.1 (2026-08-07) — 简化分类
- 去掉"在用"中间层：活跃项目直接放 `游戏/` `工具/` 根目录
- "废弃"改名"废弃方案"：更好听，有完成使命的感觉
- 目录结构：`游戏/xxx.md`（活跃）→ `游戏/xxx.md`（废弃方案），工具同理
- 颜色：`tag:#废弃方案` 灰色覆盖 `path:游戏/` `path:工具/`

### v2.2 (2026-08-07) — 每日推送 workflow_dispatch 触发修复
- **踩坑**：GitHub Actions YAML 解析器 bug——`run: |` 中包含 Python 代码会导致 `workflow_dispatch` 触发器被忽略（HTTP 422）
- **修复**：Python 代码外置为 `scripts/daily_analyze.py`，workflow 只调用 `python3 scripts/daily_analyze.py`
- workflow 成功端到端运行：新闻采集 → 健康检查 → DeepSeek AI 分析 → 微信推送 → 自动 commit
- 仪表盘 `index.html` 目录引用修正：`每日早报/` → `日志/`

### v2.3 (2026-08-07) — 每日推送 AI 分析深度优化
- **三大优化**：①每条新闻三维度分析（发生什么/为什么重要/跟我有啥关系）②重要性★★★评分系统 ③项目状态中文化（不再显示HTTP裸码）
- **踩坑**：Python 3.12 runner 上 emoji 字符和中文引号嵌套导致 SyntaxError，两次失败后改为 ASCII-safe 的 list-append 方式构建 prompt
- 已验证端到端运行成功，报告质量大幅提升

### v2.4 (2026-08-19) — Obsidian 图谱全量整理
- **背景**：分类规则 08-16 才定，之前无存量整理，积累 17 个重复文件 + 5 个错分类
- **错分类归位**：`goal命令`/`第二大脑` → 资料/；`记忆系统`/`全局指令优化`/`AIGC美术方案` → 项目/；`Obsidian-Copilot配置` → 资料/（指针文件与完整版合并）
- **日报归位**：废弃 `日报/` 目录，早报/日报统一进 `日志/AI日报记录/`（08-17/18 补位）
- **去重**：24 个重复/会话变体文件移入 `_垃圾站/`
- **用户画像**：人物画像/语言风格/关注主题 三件套互链成簇
- **图谱显示**：graph.json 排除 `日志/`、`_垃圾站/`、`日志/AI日报记录/`、`仓库/Claude Code 历史/`
- **断链**：5 类旧路径链接（指向已移文件）全修复，核心目录断链清零
- **规则沉淀**：CLAUDE.md v4.5 明确「早报/日报 → 日志/AI日报记录/，不放 日志/」
- **踩坑**：git ls-files/status 输出中文路径默认转义成 `\xxx\xxx`，直接 grep 中文匹配不到，易误判跟踪状态；判断中文文件是否被跟踪要用 `git status --short` 或按转义形式查

### v2.5 (2026-08-19) — 目录重组：日记→日志，AI日报并入日志
- **背景**：用户要求「日志转移到日记里，日记改名日志，日志改名 ai日报记录」，确认后定稿：进度流水账归 `日志/`，AI日报作为 `日志/AI日报记录/` 下级子目录
- **改动**：`git mv 日记 日志` + `git mv 资料/日志 日志/AI日报记录`；全库 sed 批量更新 wikilink（`[[日记/`→`[[日志/`、`[[资料/日志/`→`[[日志/AI日报记录/`）
- **联动更新**：CLAUDE.md v4.7（目录树/分类逻辑/边界判断/颜色表/白名单）、MEMORY.md v4.7、graph.json（排除 `-path:日志`，颜色 `path:日志/`）、daily-brief.yml（日报输出路径 `日志/AI日报记录/`）、说明.md 系列、记忆系统 v2.5
- **坑**：`资料/日志.md` 空文件（目录名冲突历史残留）未删（分类器拦截自主删除，等用户决定）；sed 替换 `资料/日志` 不带尾斜杠的形式会漏（如 `mkdir -p 资料/日志`），需两种形式都替换
- **断链**：重组后全库 `[[日志/` 链接 22 条全部 OK，`[[日志/AI日报记录/]]` 目录型链接改纯文本

### v2.6 (2026-08-28) — Obsidian 保守整理
- **范围**：按保守整理方案执行，不永久删除、不大规模移动、不碰 `系统/` 密钥目录。
- **索引**：MEMORY.md 升到 v4.12，补近期 AI 日报、Claude Code 历史总索引、口播工作流备份说明、整理候选清单。
- **frontmatter**：正式目录 47 个旧文件补齐/规范化 frontmatter，重点是顶层 `tags`、日报最小头部、Claude Code 历史会话 `name/description`。
- **资料关系**：`资料/Obsidian-Copilot配置.md` 定为 Copilot/RAG 主文件；`资料/Obsidian-RAG配置指南.md` 标为旧版快速指南；`资料/第二大脑.md` 改为当前系统说明，历史演进仍留本文件。
- **候选清单**：新增 `待办/2026-08-28-第二大脑整理候选.md`，列出垃圾站可删除候选、需人工确认文件、合并候选和不建议合并对象。
- **协作记录**：已尝试分派 4 个子 Agent；合并建议员返回有效结论，其余 3 个因 Codex 用量限制失败，后续验收改由本地脚本完成。

## 相关

- [[资料/第二大脑]] — 记忆系统的 Obsidian 可视化端
- [[项目/全局指令优化]] — 自动存档规则在此定义

### v2.7 (2026-09-11) — 保存路径唯一性与旧规则清理（历史，已被 v2.9 替代）
> ⚠️ 本节记录当时方案，不再作为当前执行规则；当前 Drive 规则只认 v2.9。
- **问题**：不同 AI 保存时偶发把内容写到 `02 Areas`、`日记/` 或错误存储位置。根因是当前目录规范与 PARA 参考方法、WorkBuddy 旧措辞、历史目录残留同时存在，模型可能把“方法论”误当“物理目录”。
- **唯一权威**：实际现存目录 + 当前根目录 `CLAUDE.md`；`MEMORY.md` 为索引。技能/资料里的方法论、历史日志和旧会话不得覆盖真实路径。
- **当时规则（已废弃）**：曾保留 `03_第二大脑备份` 这一概念；2026-09-13 起已取消，Drive 只作为 `E:\第二大脑` 的同步读写介质。
- **目录白名单**：正式顶层知识分类固定为 `项目/ 知识/ 技能/ 资料/ 日志/ 仓库/ 待办/ 灵感/ _垃圾站/ 系统/`。未经用户确认禁止新增 `01 Projects/02 Areas/03 Resources/04 Archives/日记` 等别名。
- **文件修复**：`CLAUDE.md` v4.14、`MEMORY.md` v4.35、`技能/PARA知识库方法.md` v1.1、`资料/WorkBuddy.md` 已同步修正。
- **残留清理**：错误 `日记/` 目录及其重复日志已不再存在；正式进度统一写入 `日志/YYYY-MM-DD-进度.md`。


### v2.8 (2026-09-11) — 保存进度执行链路简化
- **触发问题**：一次“保存进度”被实现成 Base64 → Blob → Tree → Commit → Ref 的 Git 底层对象链路，用户看到大量难以理解的内部步骤且流程长时间未完成，主动打断。
- **判断**：Blob/Tree 本身不是知识库垃圾文件，但对日常保存属于不必要的底层实现，增加卡住、误操作和用户无法判断状态的风险。
- **新默认流程**：定位真实主库和已有文件 → 更新/追加 → 检查改动 → 使用文件级 GitHub 更新/提交 → 验证远端结果。
- **限制**：日常保存不得为了“合并成一次提交”等目的主动做 Base64 编码、手工创建 Blob/Tree 或直接改 ref；仅当常规文件级方式确实不可用时才允许考虑，并且必须先向用户说明。

### v2.9 (2026-09-13) — Drive 单一用途与错误位置彻底收敛
- **唯一主库**：`E:\第二大脑`。Google Drive 只有一个作用：作为 ChatGPT 读写本机第二大脑的同步介质。
- **唯一 Drive 入口**：`我的笔记本电脑/第二大脑/`，其内容对应 `E:\第二大脑`。My Drive 根目录下不再维护第二套项目区、备份区或知识库。
- **取消分级**：删除 0-4 级“低消耗分级执行”全局规则及对应知识条目；模型/额度策略不再污染知识库路径规则。
- **错误目录复核**：旧 `日记/` 仍实际存在，且 2026-09-12 文件含正式 `日志/` 中缺失的 A0.3、P1、P1.1、P2、N0.2 实施与冻结记录；因此先无损合并，再删除旧目录，纠正此前“已不存在”的错误记录。
- **AI短剧归位**：`AI短剧_失踪者名单` 已归入 `项目/`；原生 Google Docs 内容转换为 `.md` 后再删除 Docs 副本，确保能真实同步回 E 盘 Obsidian 主库。
- **My Drive 清理原则**：先确认内容已进入第二大脑，再删除空的/错误的 `ChatGPT/`、`00_重要资料/`、`01_照片备份/`、`02_项目资料/`、`03_第二大脑备份/` 等临时层级。

### v3.0 (2026-09-13) — 本地与云端全量核验及剩余独有内容保全
- 只读递归盘点正式知识分类、历史归档和垃圾站；盘点到 205 份 Markdown，逐份正文与本地主库比较，一致 205 份、差异 0 份（忽略 BOM、换行符和末尾空白）。工具配置与系统密钥未纳入正文核验。
- 新发现待办目录中两份原生 Google 文档，本地只有 `.gdoc` 快捷文件。全文保全到 [[待办/AI收件箱（待办项目）]]、[[待办/工作流复制器（待办项目）]]，云端读回确认全文存在后删除两份原件。迁移前原文保留在本次工作区隔离快照中。
- 《失踪者名单》13 个模块目录、8 份 Markdown 全部存在，8 份正文两端一致。空模块属于项目导航明确预留，保留。
- 从 Git 历史核对旧日记两份：9 月 9 日和 9 月 12 日有效事实均已保全到正式日志，无需保留旧物理目录。
- My Drive 根目录为空，非回收站中的错误 ChatGPT、日记、02 Areas、01 Projects 及旧备份分类未再检出；本轮未再次删除已不存在的目录。
- 通过唯一镜像完成本地新建上传、云端修改回传实测，两方向通过。保留原有同步映射并原地收敛，无需清空正式镜像后重传。
- 参考：Google 官方说明 https://support.google.com/drive/answer/10838124 ，编辑、移动、删除会在同步目录两端传播，因此仅删除已核实被 Markdown 替代的原件。

### v3.1 (2026-09-13) — 验收边界与保存流程复核
- 当前正式 Markdown：两端各 208 份，两端独有路径均为 0；本轮 17 份关键正文读回比较，差异 0。历史原件及评论版本、系统密钥和工具配置不在本轮全量验证结论内，不承诺绝对无遗漏误删。
- 日常保存固定为“定位已有文件 → 拉取最新 → 更新对应文件和版本 → 追加当天日志 → 按需更新索引 → 检查变更 → 按文件提交 GitHub → 等待 Drive 同步并读回比较 → 分别报告三处状态”。不夹带清理或目录重建。
- 遇到同步或提交失败时保留进度，准确报告已完成与未完成阶段，禁止以云端写入成功代替本地同步成功。
