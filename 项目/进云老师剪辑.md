---
name: jinyun-video-editing
description: 教育IP口播剪辑，AI辅助自动化方案
tags: [视频剪辑, AI, 自动化, 进云老师]
metadata: 
  node_type: memory
  type: project
  status: 活跃
  version: v1.13
  modified: 2026-08-20
  originSessionId: 97234303-2b96-4c4c-8a01-e4743f297fee
---

# 进云老师剪辑

## 2026-08-18 真实素材工作流验收

- 已搭建并运行 PC 端口播自动粗剪 v1.0：FFmpeg + whisper.cpp + Python 脚本。
- 输入素材：`C:\Users\Administrator\Documents\xwechat_files\wxid_ar8lbm6yzxlm22_d209\msg\video\2026-08\266fcfd9ef4b3c2ac7c0813706b4130a.mp4`
- 素材规格：64.066667 秒，720×1280，30fps，9:16，含视频和音频流。
- 已识别并生成审核稿、EDL 剪辑清单、重点候选和预览视频。
- 自动识别结果：164 个逐词 token，2 个剪辑候选（长停顿 0.58 秒、语气词“啊”），3 段保留区间。
- 预览输出：`C:\Users\Administrator\Documents\xwechat_files\wxid_ar8lbm6yzxlm22_d209\msg\video\2026-08\edit\preview.mp4`
- 预览时长：59.150333 秒；视频流 59.122331 秒；音频流 59.150333 秒；分辨率 720×1280；音视频流均存在。
- 已抽帧检查首帧、中段、末帧：人物存在、无黑帧、无明显画面错位、竖屏构图正常。
- 渲染模式：默认 `copy` 保持原始码流、速度快；新增 `reencode` 安全模式，使用 concat 滤镜重新编码为 H.264 + AAC，可消除拼接 DTS 警告。
- 安全预览：`C:\Users\Administrator\Documents\xwechat_files\wxid_ar8lbm6yzxlm22_d209\msg\video\2026-08\edit\preview-safe.mp4`，时长 59.120000 秒，DTS/错误警告数 0。
- 字幕功能：根据 EDL 输出时间轴生成 `master.srt` 和带样式的 `master.ass`；真实素材生成 23 条字幕。
- 带字幕成片：`C:\Users\Administrator\Documents\xwechat_files\wxid_ar8lbm6yzxlm22_d209\msg\video\2026-08\edit\final-safe.mp4`，时长 59.120000 秒，720×1280，字幕中文字体成功加载。
- 已将同一份已验收成片复制为标准交付文件：`C:\Users\Administrator\Documents\xwechat_files\wxid_ar8lbm6yzxlm22_d209\msg\video\2026-08\edit\final.mp4`。
- 当前边界：默认 `copy` 模式仍可能出现 Non-monotonic DTS 警告；交给剪映、Premiere 等软件继续剪辑时使用 `-RenderMode reencode`。
- 暂未实现：重点前置、B-roll/贴图、特效、剪映工程导出、近义重复识别增强。

## 2026-08-19 v1.4 升级：AI 层 + FunASR + 剪映导出

调研后按用户决策落地（DeepSeek API 统一做语义判断、转录换 FunASR、5 项增强全做、剪映导出）：
- **转录换 FunASR**：SenseVoiceSmall 中文最佳、字符级时间戳（实测毫秒、相对整段）；子进程调 Python3.11 执行，避免多环境冲突；回退链 whisper.cpp → faster-whisper → openai-whisper。
- **AI 层（DeepSeek，可 -NoAi 关）**：近义重复、口误/残句识别，失败自动回退机械层；机械层新增 videocut 式「开头≥5字删较短句」规则。
- **重点前置**：DeepSeek 按 6 种钩子模板（痛点/反常识/数字/场景/承诺/结果先行）选开场，hook_candidates.json 输出。
- **字幕语义断句**：8-10字/行、1.5-3秒/条，超长句由 DeepSeek 在自然断句处拆分（失败回退硬切）。
- **渲染**：默认 reencode + 段边界 30ms afade 防爆音 + 渲染后 DTS 单调性自检。
- **剪映草稿导出**：pyJianYingDraft 0.3.0，`edl+SRT → 剪映草稿`，主视频轨按保留区间排列 + 字幕轨。

### 8/19 真实素材验收（同 64s 素材）
- 转录：backend=funasr，195 字，字符级时间戳 ✅
- 分析：run-report v1.1，ai_enabled=true，17 候选 / 13 cuts / 1 hook，AI 识别出近义重复 + 口误各 1 ✅
- 字幕：28 条，语义断句，10字/行 ✅
- 渲染：reencode 40.94s 成片，**DTS 非单调 0 / 1226 包** ✅
- 烧录：final.mp4 40.94s，720×1280，含音视频流 ✅
- 剪映草稿：14 段、40.89s 时间轴、video+text 双轨道，draft_content.json+draft_meta_info.json ✅
- 坑：run.ps1 必须带 UTF-8 BOM，否则 PowerShell 5.1 按 ANSI 读中文乱码解析失败。

### 当前边界（v1.4）
- 剪映草稿素材引用原视频文件绝对路径，原文件不能移动。
- 剪映 11.2 已装（D:\JianyingPro\11.2.0.14339\），明文草稿可直接打开，无需降级 5.9。
- B-roll/贴图/特效仍待做。
- 长音频（>几分钟）FunASR 未按段切分，时间戳精度可能下降（当前素材 64s 无影响）。

## 2026-08-19 v1.5：video-cutter 全自动剪辑（Claude Code skill）

用户要求「全自动化：丢进素材→自动出成片」，调研市面 skill 后选装 **yzz05220-rgb/video-cutter**（videocut-skills 全自动升级版），装到 `D:\video-cutter\`。与 v1.4 DeepSeek 工作流并行的第二条路径，**全本地、无外部 API**。

### 一键命令
```
cd D:/video-cutter/scripts && python all_in_one.py 视频路径 --project 项目名 --remove-silence --no-gifs
```
自动完成：转录(FunASR)→分析→剪辑(FFmpeg)→字幕→金句→统计。

### 本地化改动（3 文件）
- `config.yaml`：金句 AI 规则 enable:false（纯本地，不依赖 LLM API）
- `all_in_one.py`：修 UTF-8 包装器崩溃、双重扩展名、字幕改走新脚本
- `subtitle_from_transcript.py`（新建）：FunASR 逐字时间戳 + filter.txt 保留区间 → 成片时间轴 SRT，按区间重映射 + 标点断句

### 真实素材验收（64s 素材，EXIT=0）
- 64.1s/12MB → 49.2s/5.7MB，h264+aac 720×1280 30fps
- 字幕 15 条，时间轴全部在成片内，无重复
- 带字幕成片：`D:\video-cutter\Projects\test_jinyun\output\带字幕_266fcfd9ef4b3c2ac7c0813706b4130a.mp4`
- 坑：parse_ranges 会误匹配 atrim（区间解析两遍）；FunASR 偶发段错误（预热后稳）；脚本单独跑要 PYTHONIOENCODING=utf-8

### 当前边界（v1.5）
- 剪气口靠静音检测+语气词规则，无 AI 语义判断（v1.4 才有 DeepSeek 语义层，两条路径互补）
- 金句 GIF 未验证（--no-gifs 关闭）
- 大批量素材未试（batch 模式待测）

## 2026-08-19 v1.6：口播工作流成品质量修复（v1.4 路径）

用户剪映 11.2 打开草稿验收发现成品三问题：第一气口太紧、没有逻辑、中段打断空白。**三问题同一根因**：原视频开头 11s 静音（0-11.09s，ffprobe silencedetect 证实）+ 结尾 4s 静音未被检测，且 hook 前置把 0-12.21s 静音段顶到成片第二段 → 成片 3s 语音 + 11s 空白。

### 修复（`口播自动剪辑工作流-v1.0\scripts\analyze_transcript.py` v1.1→v1.2）
1. **开头/结尾静音检测**：首个词之前/末词之后的静音 ≥0.5s 整体删除（edge_silence 候选）；原来只测词间停顿，漏掉词外大静音
2. **edl source_duration 用 ffprobe 真实时长**（64.07s）：原来取最后词 end=59.91，尾部 4s 内容被丢弃
3. **AI 层禁止删除开场句**：DeepSeek 曾把 hook「姐妹们千万不要再给这四种食物」判为重复要删，现 index 0 的删除建议一律跳过
4. **人工审核修正 AI 误判**：AI 把「哪天想不起来了还能找出来兑一兑」判为口误删 8s（21.99-29.91），实为完整句（兑一兑=再看一眼），review.md 改 KEEP 恢复

### v1.6 验收（同 64s 素材）
- 成片 39.21s，**全片无 >0.8s 静音**（原 40.94s 含 11s 空白）
- DTS 非单调 0/1174 ✅
- 37 条字幕（比 28 条多，内容完整了）、16 段剪映草稿、39.18s 时间轴
- 剪映 **11.2 明文草稿可直接打开**（不用降级 5.9；pyJianYingDraft 0.3.0 生成新版格式兼容，视频素材引用原文件绝对路径仍有效）

### 遗留
- AI 层稳定性：DeepSeek 对口语词（兑一兑）误判、同素材重复跑结果可能漂移 → 靠 review.md 人工审核兜底，必要时用 -NoAi 纯机械层
- 原视频 0-12s 静音（可能拍摄前的空镜），此类开头空白现在自动剪掉

## 2026-08-19 v1.7：气口修复（长停顿保留呼吸）

用户反馈 v1.6 成品「气口太紧、上一句话最后一个字没说完、听感卡顿」——根因：长停顿候选区间 = 整段停顿 `[prev.end, current.start]`，CUT 后整段删光，两句话 0 空隙贴死；且剪切点正好压在尾字 `end` 时间戳上，尾音被硬掐。

### 修复（`analyze_transcript.py` v1.2→v1.3）
1. 长停顿**只删中间过长部分**，前后各留 `BREATH=0.15s` 自然呼吸（两段间 0.3s 气口）；剪切点随之前移到 `prev.end+0.15`，不再掐尾字
2. 候选数量不变（`gap≥0.45` 条件不变），旧 review.md 决策 idx 全部对上，不用重跑 AI 层（避免 DeepSeek 漂移）

### v1.7 验收（同 64s 素材）
- 成片 **42.87s**（比 v1.6 的 39.21s 多 3.6s = 保留的呼吸时间），DTS 非单调 **0/1284** ✅
- silencedetect 双阈值验证：0.8s 阈值 **0 处**（无大空白）；0.2s 阈值 **14 处**（=14 个剪切点保留的 0.3s 自然呼吸）✅
- 38 条字幕、16 段剪映草稿、42.78s 时间轴，已覆盖剪映草稿目录（需重启剪映刷新）

> 教育IP口播剪辑项目：20分钟读稿 → 1分21秒竖屏成片

## 2026-08-19 v1.8：运镜特效 + 合成音效（口播工作流 v1.7）

用户新需求「自动化插入贴图和特效音效」，本阶段落地运镜特效 + 合成音效；贴图待用户提供剪映素材库 resource_id，实拍素材（小红书/抖音抓取）封号/版权风险已搁置。

### 技术确认
- pyJianYingDraft 0.3.0：`add_effect`（VideoSceneEffectType 含推拉/_360/跟随/缩放/S 形等运镜）+ `AudioSegment`（本地音效 volume 可控）实测可用；**特效/音效轨道必须手动 append_track**（_get_track 不自动创建）
- 音效素材剪映本地无独立源 → **ffmpeg 本地合成**（whoosh 白噪声+带通+包络、ding 正弦+衰减），无版权免费

### 改动（工作流 analyze v1.3→v1.4、整体 v1.6→v1.7）
1. `analyze_transcript.py`：AI 层额外输出 `key_sentences`（2-4 个重点句）写入 edl.json
2. `plan_effects.py`（新）：重点句原片时间 → 成片时间（keep_ranges 拼接）→ 每个重点句 1 条运镜（0.8s）+ 前 2 个重点句 whoosh/ding 音效（0.5s 音量 0.4）；无 AI 重点句时机械回退（长句+强调词）；特效/音效防重叠
3. `gen_sfx.py`（新）：ffmpeg 合成 whoosh.wav/ding.wav，幂等
4. `export_jianying.py`：读 effects.json 建特效轨/音效轨；单条失败只警告不中断
5. `run.ps1`：`-Effects` 开关（配合 -Jianying）
6. `README.md`/`SKILL.md` v1.7

### 验收（同 64s 素材，临时目录）
- whoosh 1.0s / ding 0.6s 可解码 ✅；机械回退正确（key_sentences_source=mechanical）✅
- 3 重点句成片 9.0/19.38/33.84s 在 [0,42.78] 内，特效/音效不重叠 ✅
- 草稿 tracks=video(16)/text(38)/effect(3)/audio(2)，运镜 @8.8/19.18/33.64s、音效 @8.7/19.08s 音量 0.4 ✅

### 坑
- export 的 apply_effects 独立函数访问不到 main() 局部 import → 函数内补 import ✅

### 待用户配合
- 关剪映后跑 `run.ps1 -Source … -Jianying -Effects` 真实验收运镜+音效
- 贴图：用户先建含贴图的剪映草稿，提取 resource_id 后自动复用

### 19:56 真实素材验收（关剪映后 run.ps1 全流程）
- `-Jianying -Effects -DeployDir` 跑通：转录→AI分析→hook→字幕38→渲染42.87s(DTS 0/1284)→音效合成→特效规划→草稿部署
- 重点句**来自 AI 层**：转折句@27.42s / 强调句「千万不能二次加热」@30.36s / 总结句「您都记住了吗」@41.82s
- 3 运镜（推拉/360/跟随 0.8s）+ 2 音效（whoosh/ding 0.5s 音量0.4，只配前2重点句），全落在42.78s内不重叠
- 已部署剪映草稿库，draft_content.json 含特效轨+音效轨+materials.audios ✅
- 事故：主目录 run.ps1 曾丢失（备份目录里是旧版），已重写并补 UTF-8 BOM（PowerShell 5.1 无 BOM 读中文会解析失败）

## 2026-08-19 v1.9：video-cutter 字幕样式（50%透明黑底修复 + 字号/时间轴迭代）

用户对 video-cutter 成片字幕要求：白字 + 50% 透明黑底 + 位置画面 2/3 处 + 断句≤10字 + 时间轴跟声音同步。本阶段完成黑底修复，字号/时间轴仍在调。

### 黑底修复（像素验证定位根因）
- **libass BorderStyle=3(黑底box) 下 outline=0 不绘制 box**（outline0 暗化像素 0，outline1→6110，outline2→6986）
- 修复：`scripts/srt_to_ass.py` outline 默认 0→2；ASS 头强制 `PlayResX=720,PlayResY=1280`（libass 默认 PlayResY=288，MarginV>288 字幕会推出画面）
- ASS Dialogue 时间必须带百分秒（`0:00:00.44`），写 `0:00:00` 报 "Bad timestamp" 整条跳过
- 成片 v7：`output\带字幕_266fcfd9ef4b3c2ac7c0813706b4130a_v7.mp4`（字号20白字+50%黑底，位置2/3处）

### 待办（用户最新要求）
- 字号 20→60
- 字幕仍比声音早：调 srt_align（pre_read=0.06 提前 + find_voice_start 窗口检到上句尾音）让字幕跟声音严格同步
- 全部 OK 后把 srt_align+断句+srt_to_ass+烧录固化进 all_in_one.py 一键流程
- 之后才考虑音效/贴图/特效

## 2026-08-19 v1.10：口播工作流 v1.8（蘑菇气口修复 + 变焦运镜 + 前三秒特效音效 + 心跳音效）

用户 4 需求：①特效音效主要加前三秒 ②运镜改"缓慢/快速的放大缩小"（变焦），不要"乱运" ③音效换热门同行在用的 ④修复「第一，蘑菇」气口（与上次一模一样、可复现），先找问题再解决。

### 蘑菇气口 bug（analyze_transcript.py v1.4→v1.5）
- **根因**：停顿判定只信转录时间戳 gap≥0.45s，而 **FunASR 字符时间戳有系统性偏差（end 比真实语音结束早 0.1-0.2s，字尾韵还在发）**。ffprobe silencedetect(-45dB) 铁证：cut#9[47.34,47.52] 区间内**无静音**（真静音 47.53-47.66 反而留在成片 = 删了「是」尾音、留了真静音）；cut#10 起点 48.12 压进「菇」尾音（真实语音到 48.19）。转录稳定 → 每次同样切 → "一模一样"
- **修复**：`detect_silences()` 全片 ffprobe silencedetect；gap≥0.45 时取窗口内最长静音段，**<0.4s 判假停顿不剪**；≥0.4s 剪切点取静音中段 [静音起点+0.15, 静音终点-0.15]
- **验证**（64s 素材 --no-ai）：蘑菇假停顿消失，仅 3 条真停顿 [48.339-48.468]/[51.856-52.022]/[56.074-56.613] ✅

### 特效音效 v1.8（plan_effects.py v1.7→v1.8 + gen_sfx.py）
1. 运镜池只用变焦类 `["推拉运镜","缩放运镜"]`（放大缩小），删掉 _360/S形/跟随等花哨运镜
2. 特效音效集中**成片前三秒**（hook 区）：开头 0.2s 强制 变焦特效+心跳音效；前三秒外 AI 重点句只加变焦特效点缀（全片≤3 特效），不配音效
3. 新增 **heartbeat.wav** 心跳音效（aevalsrc 低频双拍"咚-咚" 1.2s），大健康赛道 hook 专属情绪锚点
4. 音效调研结论：大健康 hook 用心跳最贴（紧张/重要感），重点句用 ding 强调；人声第一、音量 30-40、只在重点节点加 1-2 个
- **验证**：effects=推拉@0.20s(hook)/缩放@27.22s/推拉@30.16s，sfx=heartbeat@0.20s 音量0.35，全在成片42.78s 内 ✅

### 待用户
- 关剪映后跑 `run.ps1 -Source … -Jianying -Effects` 完整验收（停顿新逻辑 + 变焦 + 心跳）

## 2026-08-20 v1.12：黑绒被切修复（AI 层默认 KEEP + 真实静音校准）+ 特效音效停用

用户反馈「黑绒被切掉」——澄清后 = **内容被切**（有些句子直接被切掉）+ 气口不规范。取证确认根因：**AI 层（DeepSeek）近义重复/口误判定过度激进**，把通顺长句整段误删（「要你的老命/点赞收藏」8 秒段、「兑一兑」、「二次加热了」动词短语、「但是啊我告诉你四种食物」转折句），且剪切边界用转录时间戳（FunASR end 系统性偏早 0.1-0.2s）导致气口不规范。同时用户明确「不要推拉/缩放运镜（日常用关键帧自做动画）」，运镜+音效全停。

### 修复（analyze_transcript.py v1.5→v1.6 + plan_effects.py v1.8→v1.9）
1. **AI 层默认 KEEP**：近义重复/口误候选默认 `[KEEP]`，需人工在 review.md 改 `[CUT]` 才删；AI prompt 加保守约束（禁止删动词短语/连接词开头句子、flub 宁可不报）
2. **全部 cut 类型边界统一真实静音校准**：`safe_cut_range()` 前边界找包含 start 的静音段或前 0.4s 内结束的静音段、后边界对称，边界校准到静音中段留 `BREATH=0.15s` 呼吸；找不到静音支撑返回 None 放弃剪切
3. **语气词只删独立停顿词**：前后都有 ≥0.3s 停顿（`FILLER_GAP_MIN`）才删；嵌句中连读语气词（「虽然啊」「但是啊」）保留
4. **edge_silence 专用校准**：开头 = 首字前最近静音段终点−BREATH，结尾 = 末字后最近静音段起点+BREATH，不切首字/末字
5. **特效音效停用**：`plan_effects.py` 加 `DISABLED=True`，输出空 effects，剪映草稿不带特效轨/音效轨；代码保留，日后恢复改 False

### v1.12 验收（同 64s 素材，python 驱动重跑全流程）
- 分析：12 候选 → **5 cuts**（AI 4 处误删全消失），全可信剪切（3 条真停顿 [48.339-48.468]/[51.856-52.022]/[56.074-56.613] + 开头 11.79s + 结尾 3.92s 静音）
- 成片 **47.57s**，DTS 非单调 **0/1426** ✅
- 字幕 39 条；剪映草稿 video+text 双轨（无特效/音效轨），4 段、47.52s 时间轴
- 修验收 bug：`detect_silences` 对不存在文件返回空列表会把 pause 候选全丢弃（verify_workflow 的 detects_pause=false）→ ffprobe `returncode!=0` 时返回 None 回退旧逻辑

### 关键改动文件（口播自动剪辑工作流-v1.0）
- `scripts/analyze_transcript.py` v1.6
- `scripts/plan_effects.py` v1.9（DISABLED=True）
- `README.md` / `SKILL.md` v1.9

### 坑/备忘
- analyze_transcript.py 碎片化 Edit 超 8-9 次会被 token-guard hook 拦 → 改整文件 Write 重写
- AI 层保守化后 review.md 说明：「机械层默认 CUT；AI 层默认 KEEP，改 `[CUT]` 才删」

## 2026-08-20 v1.11：验收通过（蘑菇气口修复确认 + 特效音效就位）+ run.ps1 丢失事件

### 验收结果（全流程重跑，零报错）
- 转录 195 字（funasr）→ 分析 12 候选 → 10 cuts → 9 keep 段 → 成片 **32.886s**
- 特效：推拉@0.20s(hook) + 缩放@17.86s + 推拉@31.73s；音效：heartbeat@0.20s 音量0.35（AI 层 key_sentences 正常）
- 字幕 24 条；剪映草稿 9 segments，720x1280，30fps

### 蘑菇气口修复确认（对比铁证）
- 旧版蘑菇区 2 条假剪切 [47.34,47.52]+[48.12,48.6]（掐「菇」尾音）
- 新版只剩 1 条真停顿 [48.339,48.468]（真实静音 0.43s，保留 0.3s 呼吸），剪切点落在静音中段

### ⚠️ run.ps1 再次丢失（第 2 次）→ 已恢复并双备份
- 8-19、8-20 两次丢失（单文件消失，原因不明）。已从会话 jsonl 提取 v1.7 完整内容恢复（UTF-8+BOM）
- 备份：项目内 `_backup\run.ps1.v1.7.20260820.ps1` + `E:\第二大脑\仓库\口播工作流备份\`（含说明.md：丢失历史/恢复方法/部署位置）
- 剪映草稿库已更新：新草稿已拷入 `com.lveditor.draft\口播自动剪辑`，旧草稿备份为 `口播自动剪辑_旧版备份_20260820_105012`

### 技术备忘
- PowerShell `-File` 中文路径必挂（GBK 编码）；git bash 的 /tmp 与宿主 Temp 视图不一致（临时脚本不可靠）→ 用 **python 驱动复刻 run.ps1 调用序列**最稳（Python311 含 funasr+pyJianYingDraft）

## 2026-08-20 v1.13：video-cutter 字幕 v8（字号60 + 字幕比声音早修复）

video-cutter 线（全本地）字幕样式定稿：白字 SimHei + 50% 透明黑底 + 位置画面 2/3 处 + 断句≤10字 + 字号 60 + 时间轴跟声音同步。

### 时间轴修复（用户反馈「字幕还是比声音早」）
- 量化诊断 32 条字幕 start 是否在语音中 → 1 条早 650ms（「不能二次加热。」）
- 根因：`scripts/srt_align.py` 的 `find_voice_start` 取窗口内**最早**语音段，误检上一句尾音当本句起点
- 修复：改取**离参考时刻最近**的语音段起点（就近回溯本句真起点）+ `pre_read` 0.06→0（不提前显示）
- 验证：#25 修复，仅剩结尾无语音边缘条；成片 `output\带字幕_..._v8.mp4`（49.06s）

### 待办
- [ ] 用户验收 v8 后，把 srt_align + 断句 + srt_to_ass + 烧录参数固化进 `all_in_one.py` 一键流程
- [ ] 之后再考虑音效/贴图/特效

## 2026-08-20 v2.0：口播工作流贴图/B-roll 上线

用户选定全做：贴图两种都要（剪映贴纸+本地图片）、B-roll 自动生成文字卡、三式文字卡配置可切换，渲染成片看效果。

- **新增 3 脚本**：`gen_textcard.py`（PIL 三式文字卡 bar/white/color，自动标题化去语气词）、`plan_overlays.py`（重点句→overlays.json，复用 map_to_timeline）、`render_overlays.py`（ffmpeg overlay 烧录 final-overlays.mp4）
- **`export_jianying.py`** 加 `--overlays`：贴纸轨/贴图轨/文字卡轨（StickerSegment/VideoSegment）
- **`overlays_config.json`**：`textcard.style` 切样式（bar/white/color）+ `color_final` 末句强化 + `images`（本地图片）+ `stickers`（剪映贴纸 resource_id）
- **验收**：2 文字卡（bar@29.28s + color@32.46s 末句）、烧录抽帧 30s/33s/38s 全对、草稿 tracks 主视频4+字幕39+文字卡轨2、已部署剪映草稿库（v1.12 备份于 `口播自动剪辑_v1.12备份_20260820_2012`）
- **待用户**：看 `edit\final-overlays.mp4` 反馈样式；提供本地图片填 `images`；剪映放贴纸后我提取 resource_id 填 `stickers`

## AI 自动化升级方案

### 当前流程（手动）

```
录制(20分钟) → 手动听写 → 手动剪辑 → 手动加字幕 → 导出(1分21秒)
                 ⏱️ 耗时约 2-3 小时
```

### 升级后流程（AI 辅助）

```
录制(20分钟) → AI自动转字幕 → AI自动剪气口 → AI粗剪 → 人工精修 → 导出(1分21秒)
                                    ⏱️ 耗时约 30-40 分钟
```

## 具体工具推荐

### 🥇 首推：快剪辑（PC 端，360 出品）

**为什么它最合适：**
- ✅ 完全免费，无广告无水印
- ✅ 自动语音转字幕（支持普通话/粤语/四川话）
- ✅ AI 自动剪气口（识别并标记无声段、重复句、语气词）
- ✅ 配置要求极低（i3 + 4G 内存即可跑 1080P）
- ✅ 操作简单，10 分钟可完成一条短视频

**下载：** 搜索「快剪辑」官网下载 PC 版

**工作流：**
1. 导入 20 分钟录制视频
2. AI 自动识别语音 → 生成字幕
3. 查看 AI 标记的"气口"（沉默、嗯啊、重复） → 勾选删除
4. 拖动时间轴精修关键段落
5. 一键导出 9:16 竖屏

### 🥈 备选：度加剪辑（手机端，百度出品）

**优势：**
- ✅ 完全免费，手机操作
- ✅ AI 字幕准确率 95%+
- ✅ 一键包装（自动加标题、花字、贴纸）
- ✅ 53MB 安装包，配置要求低

**适用场景：** 出门在外想快速剪、或者习惯手机操作

### 🥉 进阶方案（等你熟练后）

**SuperAI 影视剪辑**（开源免费桌面应用）
- 视觉推理分析视频帧
- 自动生成解说脚本
- 支持批量处理
- ⚠️ 需要 Python + Node.js 环境，配置稍复杂

## 字幕优化建议

| 当前问题 | AI 解决方案 |
|---------|------------|
| 老师说话快、有口音 | 快剪辑支持方言识别，准确率够用 |
| 专业术语识别差 | 手动添加"热词库"（学科术语表） |
| 字幕样式单一 | 快剪辑内置多种字幕模板，一键套用 |
| 字幕时间轴不对 | AI 自动对齐，支持手动微调 |

## 效率提升预估

| 环节 | 当前耗时 | AI辅助后 | 节省 |
|------|---------|----------|------|
| 转字幕 | 30分钟（手动听写） | 2分钟（AI自动） | 93% |
| 剪气口 | 40分钟（手动找） | 5分钟（AI标记+确认） | 87% |
| 精修 | 30分钟 | 15分钟 | 50% |
| 加字幕特效 | 20分钟 | 3分钟（模板套用） | 85% |
| **合计** | **~2小时** | **~25分钟** | **79%** |

## 实施步骤

1. **本周**：安装快剪辑，试剪一条旧素材，熟悉AI字幕+气口功能
2. **下周**：新录制素材全部走 AI 流程，记录实际耗时对比
3. **两周后**：根据使用体验决定是否升级到 SuperAI 或保持快剪辑

## 注意事项

- AI 字幕准确率不是 100%，专业术语需要人工校对
- AI 粗剪后必须人工过一遍，避免误删关键内容
- 手机和 PC 工具可以配合使用：PC 粗剪 → 手机精修加特效
- 所有工具都免费，先别花钱买会员

## 2026-08-30 ~ 2026-08-31 v10.0 剪映原生底层引擎与 Web 工作台全要素重构

在 `E:\项目\autoclip-web\` 完成全自动短视频口播剪辑 Web 工作台重构，直接输出至剪映 Pro 11.3 原生草稿。

### 核心技术突破
1. **剪映原生底层 JSON 纯净克隆**：彻底废除第三方封装库（pyJianYingDraft），直接以原生 dict 结构构建合法的 `draft_content.json` 与 `draft_meta_info.json`，解决图层重复与属性丢失问题。
2. **绝对零时移声画毫秒级咬合 (Zero-Offset Sync)**：说话者在 0.42s 开口，主字幕即在 0.42s 毫秒级弹出，杜绝任何人工延后的时序漂移。
3. **字幕全篇 100% 绝对无缝相连 (0.000s 空隙)**：`segment[i].end === segment[i+1].start`，全片彻底消灭黑屏闪烁与断流。
4. **主字幕单轨行内局部改色 (In-line Range Accent)**：通过 `styles[range]` 实现单条字幕内对关键数字（如“四种”、“110到130”）进行**浅黄色 #F9F3C4（[0.976, 0.953, 0.769]）**改色，不产生多余图层，彻底废除粉色。
5. **极简工程命名与自动部署**：草稿统一命名为 `MMDD-HHMM`（如 `0831-1135`），并自动部署至剪映草稿目录 `com.lveditor.draft`。

### 真实标杆参数字典 (1:1 来自 extracted_open_project.json)
- **字体资产**：
  - 主字幕：`俪金黑`（ID: `6740499317733200388`），字号 12.0，`y = -0.357052`，阴影 `alpha: 0.4835`
  - 顶部大标题：`字语咏楷体`（ID: `7312719210373714459`），字号 15.0，纯白，`x = 0.0, y = -0.3333`
  - 中部提醒大字：`方正风雅宋`（ID: `7208056673317950011`），字号 15.0，放大 1.37x，浅黄色 `#F9F3C4`，`x = -0.17, y = -0.21`
  - 结尾升华大字：`俪金黑`，字号 12.0，放大 1.49x，浅黄色，`x = 0.45, y = -0.24`
- **音效与BGM体系**：
  - `0.00s` 亮一下 (`1e326c72...mp3`)
  - `1.02s` 眼前一亮 (`c4af9bf8...mp3`)
  - `2.80s` 大明王朝悬疑 (`3b3c79a5...mp3`)
  - `46.32s` 仙尘音效 (`3b06bd9e...mp3`)
  - `0.00s ~ 47.52s` Peace BGM (`23d0d480...mp3`，音量 0.18 铺底)

### 当前基线与文档位置
- 项目源码工作区：`E:\项目\autoclip-web\`
- Git 稳定基线：`v10.0 (fe7c674)`
- 完整交接手册：`E:\项目\autoclip-web\HANDOVER_DOCUMENTATION.md`
- 版本变更日志：`E:\项目\autoclip-web\CHANGELOG.md`