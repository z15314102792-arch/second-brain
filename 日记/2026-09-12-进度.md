---
name: 2026-09-12-进度
description: 2026-09-12 工作与研发进度沉淀
tags: [日记, 进度, 深井, Android, Godot]
metadata:
  type: journal
  date: 2026-09-12
---

# 2026-09-12 工作进度记录

## 今日完成重点事项

### 1. 《深井》Android Build A0.1 原生可玩性与 Web/Godot 一致性修复
- **真机缺陷定位与修复闭环**：
  1. **开场人物不动**：修复 `_process` 覆盖地面位置问题，建立钢索下降严格阶段保护，落地冲击与钢索回抽演出完整恢复；
  2. **标题主页拥挤重叠**：使用标准 Godot Control 重构 `TitleScreen`，加入矿工徽标留白，阶段 0 彻底互斥隐藏地面玩家模型；
  3. **设置点击无反应与假死**：实现全新通用设置面板 `SettingsDialog`，滑块实时调控 AudioSystem 并持久化存档，开关 10 次 0 内存泄露；
  4. **进入游戏完全无法挖矿**：修复长屏物理像素到 360×640 逻辑坐标映射，加入 80ms 同触控去重，彻底解决触控+模拟鼠标双事件导致的“刚点击即取消”；
- **双套自动化测试全绿通过**：
  - 新增 `tests/test_a01_fixes.gd` 6 项专项测试 100% 通过（绳索下井、标题角色互斥、设置无泄露、设置音量保存、三向挖掘、触控去重）；
  - `tests/test_runner.gd` 41 项全量回归测试 100% 全部通过。
- **Android A0.1 APK 独立构建发布**：
  - 产物路径：`E:\项目\deep-mine\builds\android\deep-mine-a0.1.apk`（versionCode=2, versionName="v0.3.3-a0.1"，未覆盖原 A0 文件）；
  - release 签名与官方 apksigner 校验通过。
- **双端 25 项功能一致性深度审计**：
  - 逐项对齐标题页、新游戏、继续游戏、开场演出、三向挖掘、取消与切换、三格塌落、资源掉落、工具制作、SaveSystem 等，标记一致性状态；
- **Godot Web Export P0 验证**：
  - 成功在 `builds/web-godot-p0/` 导出 Web 版本（nothreads 模式，免跨域隔离头，移动端友好）；
  - 8090 端口验证加载正常，明确技术路线：待 A0.1 手机验收与包 4 完成后，以 Godot Web Export 替代 Legacy 手写 Web 端。

### 2. AI 额度实时监控桌面看板（v3.2 修复 Gemini 额度显示 100% 已重置缺陷）
- **根因精准定位**：
  1. `parse_usage_text` 中在解析后端数据时，存在脑补的 `if ts > 0 and ts <= now_t: pct = 100` 逻辑，擅自篡改后端权威返回；
  2. 自动定时刷新调度（`_task_antigravity`）被硬编码限制为仅在用户手动点击强制刷新时触发，导致后台 30s 轮询从不主动同步 Antigravity 最新配额；
  3. 本地倒计时定时器（`_schedule_local_timer`）在上一周期重置时间戳到期后，死循环将界面强行刷成 `5h: 100% (已重置)` 并改写内存配额为 100，导致消耗速度计算分支被误判为“速度: 刚重置”。
- **精准修复闭环**：
  1. 移除 `parse_usage_text` 中的数据覆盖篡改，100% 忠实提取官方 `/usage` 返回的真实剩余百分比与时间戳；
  2. 恢复后台智能静默查询：按配置间隔（默认 60s）与到期核验（15s 重试）在后台异步拉取真实配额，有代理预检保护、0 黑框、不卡 UI；
  3. 优化本地倒计时到期感知：在拉取到新周期前平滑显示 `(同步中)`，杜绝虚假覆盖真实数值；获取到新周期后毫秒级恢复新倒计时与真实百分比；
  4. 验证通过并平滑热重启后台看板进程（PID 11932），Gemini 真实额度即时上屏（5h 剩余 80%，周剩余 63%），完全对齐 Google 后端真实数值。

### 3. 全局交付路径规范与长期规则沉淀（2026-09-12 机械强制生效）
- **机械强制规则固化**：
  - 每次生成 APK、Web 构建包或其他需要用户手动打开/安装的文件，最终报告必须同时给出 Windows 完整绝对路径（例如 `E:\项目\deep-mine\builds\android\deep-mine-a0.1.apk`），严禁只写文件名或相对路径；
  - 涉及安装或手动操作时，必须明确写：“你现在安装这个文件：完整绝对路径” 或 “你现在打开这个文件：完整绝对路径”；
- **配置与手册同步更新**：
  - 全局指令 `C:\Users\Administrator\AGENTS.md` 升级为 v2.4 并写入该规则；
  - 第二大脑操作手册 `E:\第二大脑\CLAUDE.md` 与项目笔记 `E:\第二大脑\项目\游戏\深井.md` 同步固化该长期规范；
- **本轮 APK 桌面便捷交付**：
  - 创建桌面目录 `C:\Users\Administrator\Desktop\深井APK\`；
  - 完整复制推荐安装的最终 APK 到 `C:\Users\Administrator\Desktop\深井APK\deep-mine-a0.1.apk`。

### 4. 《深井》Android Build A0.2 UI单一化、状态机收敛与手机布局修复
- **三大结构性问题根因定位与清除**：
  1. 解决继续游戏标题未退：修正过渡计时到期流转逻辑，原子化隐藏 TitleScreen 并激活 PLAYING 状态；
  2. 彻底消灭双游戏菜单：清除 main.gd _draw() 中残留的旧手绘菜单，由统一 PauseMenuDialog 独占管理；
  3. 修复设置黑板与卡死：清除旧手绘深色矩形，由标准 SettingsDialog Control 承接 3 组 Slider 调节与大按钮；

### 4. 《深井》Android Build A0.2 UI单一化、状态机收敛与手机布局修复
- **三大结构性问题根因定位与清除**：
  1. 解决继续游戏标题未退：修正过渡计时到期流转逻辑，原子化隐藏 TitleScreen 并激活 PLAYING 状态；
  2. 彻底消灭双游戏菜单：清除 main.gd _draw() 中残留的旧手绘菜单，由统一 PauseMenuDialog 独占管理；
  3. 修复设置黑板与卡死：清除旧手绘深色矩形，由标准 SettingsDialog Control 承接 3 组 Slider 调节与大按钮；
  4. 手机居中自适应：全屏 CenterContainer 封装，触控大按钮高度均 >= 46dp，彻底解决偏向左上缺陷；
  5. 双保险防穿透挖矿：物理级阻断非 PLAYING 状态的一切矿区交互；
  6. 存档简报刷新与真实位移：返回标题即时刷新数据，开场绳索下井起点校准为 -80.0。
- **61 项自动化测试 100% 全部通过**：
  - 新增 `tests/test_a02_state_machine.gd` 20 项专项断言全部通过；
  - 历史 `tests/test_runner.gd` 41 项全量测试全部通过。
- **A0.2 APK 构建发布与交付**：
  - 绝对路径：`E:\项目\deep-mine\builds\android\deep-mine-a0.2.apk`（versionCode=3, versionName="v0.3.3-a0.2"，26.75MB，release 签名验证通过）；
  - 桌面交付：已复制到 `C:\Users\Administrator\Desktop\深井APK\deep-mine-a0.2.apk`。

### 5. 《深井》Android Build A0.3 音频反馈、UI可读性、主题系统与制作页修复
- **四大核心问题精准定位与彻底攻坚**：
  1. **P0 制作页卡死/无法关闭**：
     - 根因：HUD 顶部栏在 main.tscn 中被限制为 76px 高度，子节点超出 76px 的区域无法接收触控，且 AppState 缺少 CRAFTING 独立状态，导致关闭按钮无法被点击，手机返回键失效；
     - 修复：新建独立 `CraftPanel` 直接挂载至 `CanvasLayer`（z_index=25），居中弹窗彻底摆脱 76px 裁剪；顶层状态机扩充 `AppState.CRAFTING`，打开时安全取消半进度并阻断世界点击；右上角提供 46×44dp 高对比度【✕】大按钮，底部配备备用返回大按钮，Android 返回键稳顺关闭并恢复游玩；
     - 信息重塑：标题“现场改造 / 制作”，展示当前工具（破旧镐/岩楔镐）、成长路径（破旧镐➔岩楔镐，未发现内容绝对不剧透不占位）、图纸卡片输入输出、材料进度（石材 8/12，充足高亮，不足禁用）；
  2. **P1 音效存在感极弱（实为假信号无声）**：
     - 惊天根因：AudioSystem 仅有 `sound_played` 信号，工程内没有任何 `AudioStreamPlayer` 或真实波形，原生端完全静音；且挖矿 2.4s 中仅 35% 和 70% 有裂纹声，中间 2 秒漫长静默；
     - 修复：在 `AudioSystem` 内置 16 位 22050Hz PCM 程序化物理波形合成引擎（零外部文件依赖、跨平台纯原生、内存仅 ~200KB），覆盖敲击普通岩、敲击铜矿、裂纹、爆裂、拾取、打铁、UI 等 27 种全量物理音效；建立 8 通道 `AudioStreamPlayer` 播放器池挂载场景树；挖掘循环中加入 0.48s 周期性挥镐与打击音效、飞溅火花，敲击节奏感充沛饱满；
  3. **P1 全局 UI 底框几乎不可辨认**：
     - 根因：卡片背景（0.07）与极暗底色（0.03）明度差过小，在手机长屏与 OLED/IPS 屏幕上融成一团黑；
     - 修复：建立三层清晰阶梯对比度规范（游戏背景 0.04~0.06 < 卡片 surface 0.14~0.19 < 弹窗 surface_elevated 0.20~0.28），边框厚度与对比度显著提升，卡片轮廓一眼即见；
  4. **P2 统一主题系统 ThemeManager 建立**：
     - 内置 7 套主题预设：矿灯琥珀（默认）、深井冷蓝、赤铜工坊、晶体紫脉、苔岩绿穴、工业冷灰、高对比度；
     - 支持深色（Dark）与浅色（Light）模式即时切换（浅色模式纯白卡片、深黑文字、保留主题 Accent）；
     - `SettingsDialog` 增加 7 套色块与深浅模式切换控件，全局即时生效；
     - 严格约束：地下世界矿石（石头、铜矿、硬岩、黑雾）语义颜色独立于 UI 主题，绝对保持恒定！
- **自动化测试套件 77 项全绿通过**：
  - `tests/test_runner.gd` 41 项全量测试 100% 全部通过；
  - `tests/test_a02_state_machine.gd` 20 项全量测试 100% 全部通过；
  - `tests/test_a03_theme_and_craft.gd` 16 项 A0.3 专项测试 100% 全部通过；
  - 合计 77 项断言无一失败。
- **Android A0.3 APK 构建发布**：
  - 产物路径：`E:\项目\deep-mine\builds\android\deep-mine-a0.3.apk`（versionCode=4, versionName="v0.3.3-a0.3"，大小 25.5MB，正式 keystore 签名有效，未覆盖历史旧包）；
  - 绝不向桌面复制 APK（严格执行用户关于不向桌面复制 APK 的指令）。

### 6. 《深井》Godot Web Preview P1 交付（动作节奏重做、下井/回落演出、7套主题与通知队列）
- **开发策略正式切换**：
  - 最新 Android APK 仍锁定为 A0.3，本轮机械强制禁止生成 A0.4 APK 或任何新的 Android 安装包；
  - 交付形态调整为 Godot Web Export（P1 预览版）高频试玩，待用户真机验收通过后再推进后续里程碑；
  - 状态明确标记为：**待用户Web真机验收**。
- **6 项核心功能彻底重构落地**：
  1. **重做挖矿动作节奏**：Player 确立为动力学权威，0.52s 物理挥镐循环（蓄力 0.12s、挥砍 0.10s、0.22s 命中关键帧派发 hit_frame、受力回弹 0.14s、复位 0.16s），由关键帧驱动打击声、粒子飞溅与裂纹递进；
  2. **重做破碎后下潜动作 (Commit Descent)**：破碎后 0.42s 平滑沉降进入下一作业面，落地瞬间受力 squash 弹性形变（Y=0.82, X=1.12），支持连续输入缓冲不吞操作；
  3. **新游戏下井动画真正实现**：天花板外（-80px）沿悬吊钢索垂直滑降至地面作业面（208px），真实位移达 288px，伴随身体晃动与卷扬机低频摩擦音，落地后钢索 0.4s 内极速向天花板回抽回收，1.0s 内彻底脱离消失；
  4. **继续游戏返回作业面平滑落地**：黑屏 0.5s 显示当前深度，矿工起始于高出锚点 20px（188px），淡入后 0.35s 内平滑沉回现场作业面；
  5. **7 套主题正式色板与 4 色块实时预览**：15 个色彩 Token 体系，支持深色/浅色模式即时切换，SettingsDialog 滚动呈现 7 套色块预设；地下世界矿石语义色严格恒定；
  6. **统一 NotificationManager 队列系统**：规范安全区（Y=90px, 宽295px居中），划分 LEVEL A 模态确认、LEVEL B 自适应时长通知（FIFO 队列）与 LEVEL C 浮字，HUD 彻底剥离剧情文字。
- **自动化测试 69 项 100% 绿色全过**：
  - `tests/test_p1_animations_and_notifications.gd` 8 项全过；
  - `tests/test_a02_state_machine.gd` 20 项全过；
  - `tests/test_runner.gd` 41 项全过。
- **构建发布与访问入口**：
  - Web 构建 Windows 完整绝对路径：`E:\项目\deep-mine\builds\web-godot-p1\index.html`；
  - 局域网访问地址：`http://192.168.0.95:8089/p1/`；
  - 公网试玩地址：`https://feeds-maintain-dairy-forum.trycloudflare.com/p1/`；

### 7. 《深井》Godot Web Preview P1.1 修复交付（中文乱码解决、岩石视觉自然化、碎屑物理重塑）
- **交付策略与机械铁律严格遵守**：
  - 最新 Android APK 仍锁定为 A0.3，本轮机械强制禁止生成任何新的 Android 安装包；
  - 交付形态为 Godot Web Export（P1.1 修复版）高频试玩；
  - 第二大脑状态明确标记为：**待用户Web真机验收**。
- **三大核心痛点根除与工程落地**：
  1. **P0 彻底解决中文乱码与缺字（豆腐块清零）**：
     - 排查根因：Godot 4 引擎未内置中文字形，Web Export 在浏览器沙盒环境无法调用系统字体回退，且代码多处引用已失效的 `ThemeDB.fallback_font`；
     - 修复方案：引入开源 Google Noto Sans SC 规范字体（`res://assets/fonts/NotoSansSC-Regular.ttf`，17.7MB），在 `project.godot`、`ThemeManager` 与导出预设 `include_filter` 中完成全链路绑定，为全部 UI 注入中文字体 Theme，全项目彻底清除 `ThemeDB.fallback_font`；
  2. **岩石视觉自然化，降低棋盘感**：
     - **同材质内部边界融合**：同一类岩石（例如普通岩）相邻时，内部边界不绘制分割线、不留黑缝，相接处底色完全融合，呈现连贯整体岩体；
     - **暴露边缘 1~3px 动态凹凸折线**：仅在与空洞或异质材质交界处绘制带 1~3px 哈希偏移的 polyline 参差边缘，上部暴露带微高光，下部带微阴影；
     - **6 种稳定内部纹理变体 (variant_id)**：MineGrid / MineChunk 种子与坐标哈希确定变体 (0~5)，重载后绝对一致；纹理控制在 8~14px 自然短节理，彻底删除贯穿方格全宽的横纵长直线；
     - **铜矿底色融合与嵌入式矿脉**：铜矿方块底色与普通岩 100% 融合统一，内部通过铜橙色矿脉（深色包边 + 鲜亮铜橙芯部 + 亮金高光核）自然嵌入，绝无横贯长线；
  3. **碎屑表现重做与删除“长线/长条”**：
     - **彻底废除方块塌落时的空心矩形线框与长速度线**：消除原 `draw_rect(..., false, 1.5)` 在摄像机下移时拉长导致的“两根长线”；
     - **目标格破碎物理重塑**：破碎生成 6~10 颗小碎块（宽高比 0.6~1.6，绝无细长拉伸），初速度水平散射（±10~28px/s）与轻微上跳（-15~-35px/s），0.15s 后受重力（320px/s²）加速下落并旋转，末端 25% 平滑淡出；
     - **左右格塌落两阶段演进**：阶段 1（0.08s 沉降 4~8px，实心岩体无空心框）➔ 阶段 2（碎成 8~14 颗偏大岩块明显向下高速垂直坠落，初速度 vy 110~180px/s）；
     - **舍弃格零收益与零泄漏**：左右塌落格（即使含铜）绝不结算资源、不飘字、不吸附，碎屑生命周期归零 100% 自动回收，100 格连续开采无内存与粒子泄漏。
- **自动化测试 56 项 100% 绿色全过**：
  - `tests/test_p1_1_font_rock_and_debris.gd` 15 项专项断言全部通过；
  - `tests/test_runner.gd` 41 项全量基线测试全部通过。
- **构建发布与访问入口**：
  - Web 构建 Windows 完整绝对路径：`E:\项目\deep-mine\builds\web-godot-p1.1\index.html`；
  - 本地 Web 构建目录绝对路径：`E:\项目\deep-mine\builds\web-godot-p1.1\`；
  - 局域网真机试玩地址（同 WiFi 推荐）：`http://192.168.0.95:8089/p1.1/`；
  - 公网在线试玩地址（随时可用）：`https://feeds-maintain-dairy-forum.trycloudflare.com/p1.1/`；
  - 明确声明：未生成任何新的 Android APK 安装包（最新安装包仍为 A0.3）。

### 8. 《深井》Godot Web UI/UX Preview P2 交付（全局界面规范统一、0个Emoji矢量图标、制作页抽屉重构、穿模根治）
- **交付策略与机械铁律严格遵守**：
  - 最新 Android APK 仍锁定为 A0.3，本轮机械强制禁止生成任何新的 Android 安装包；
  - 交付形态为 Godot Web Export（固定目录 `builds/web-live/`，固定访问入口 `/play/`）；
  - 第二大脑状态明确标记为：**Web UI/UX P2开发完成，待用户真机验收**（严禁写“P2正式通过”）；
- **核心成果与工程落地闭环**：
  1. **开源像素展示字体与多变体字体系统建立**：
     - 正版引入 OFL 开源像素展示字体 `FusionPixel-12px-zh_hans.ttf`（7.0MB），构建大标题（24px）复古像素工业质感，fallback 绑定 Noto Sans SC；
     - 规范 Body（450~500）、Medium（550~600）、Bold（650~700）变体体系；
  2. **10 个单色 SVG 矢量图标制作与彻底灭绝 Emoji 依赖**：
     - 自制导入 `icon_stone`, `icon_copper`, `icon_depth`, `icon_tool`, `icon_craft`, `icon_menu`, `icon_close`, `icon_arrow`, `icon_target`, `icon_settings` 10 个纯白分层矢量图标；
     - 全局所有 UI 0 个 Emoji 字符，彻底杜绝 WebAssembly Canvas 中因缺失 Emoji 字体导致的方框乱码；
  3. **HUD 极简三层规范 (高度 86px)**：
     - 第 1 行：深度数字 + 当前工具名 + 40×40 制作与菜单按钮；
     - 第 2 行：独立表面悬浮胶囊 Chips（石头/铜矿图标+数量）；
     - 第 3 行：单行超长省略号目标指引，彻底删除调试文本；
  4. **标题主页、暂停菜单与设置弹窗全面统一**：
     - 标题主页：56×56 Logo + 24px Fusion Pixel“深井” + 304px 存档卡 + 48px 按钮组，20%~33% 黄金分割布局；
     - 暂停菜单：304px 居中，圆角 10px，按钮“继续游戏”、“设置”、“返回主菜单”；
     - 设置弹窗：324px 居中，清晰分区【外观】（7套主题4色预览+深浅切换）与【声音】（三滑块）；
  5. **制作面板重构为底部抽屉 Bottom Sheet**：
     - 宽最大 336px，高 340px，贴底居中，上缘圆角 12px，半透明遮罩点击背景关闭；
     - 单色图标流展示配方关系 `[基础工具] + [材料进度] → [目标工具]`，彻底废除“成长路径”等冗长剧透；
  6. **通知系统规范**：
     - HUD 下方 12px（Y=98px 居中），宽 82%，LEVEL A 模态确认、LEVEL B 自适应 3.4~5.5s、LEVEL C 浮字 0.8~1.1s；
  7. **角色下潜防穿模彻底根治**：
     - 锁定 0.32s 黄金律时长；
     - 目标破碎后左右格先沉降（0.08~0.12s 形成 3 格安全空腔），角色微蹲蓄力 0.06s；
     - 斜向位移采用 Y 轴先沉入空腔、X 轴滞后侧滑的避角内弧线，100% 杜绝切角穿擦实心岩石；稳固 FeetAnchor 锚点；
  8. **普通岩底色明度平滑**：
     - 明度系数收敛至 0.005（±5%~7% 范围），消灭过黑突兀色块；
  9. **固定 Web 构建目录与访问地址**：
     - 构建包路径固定为 `E:\项目\deep-mine\builds\web-live\`；
     - Web 服务通过软链接固定为 `/play/`。
- **全量测试 51 项 100% 绿色全过**：
  - 新增 `tests/test_p2_ui_ux_and_antislop.gd` 10 项专项测试全部通过；
  - 基线 `tests/test_runner.gd` 41 项全量测试全部通过。
- **构建发布与访问入口**：
  - Web 构建产物 Windows 完整绝对路径：`E:\项目\deep-mine\builds\web-live\index.html`；
  - 本地 Web 构建目录绝对路径：`E:\项目\deep-mine\builds\web-live\`；
  - 局域网真机试玩地址（同 WiFi 推荐）：`http://192.168.0.95:8089/play/`；
  - 公网在线试玩地址（随时可用）：`https://feeds-maintain-dairy-forum.trycloudflare.com/play/`；
  - APK 说明：严格遵守指令，未生成任何新的 Android APK 安装包（最新正式安装包仍为 A0.3）。

### 9. 《深井》N0.2 叙事与对白大框架实现

- Codex 按用户要求亲自完成本次大框架：建立“井口先对话、主角说放绳后再下降、落地再短交流”的新开场状态机。
- 重写主角“老许”和调度对白，明确专业、谨慎、少话但会回应的人格；工具损坏、石材足够、首次铜矿、硬岩受阻、重复 31 标记均改为短句与双向交流。
- 新增轻量 DialoguePresenter，将无线电、主角念头与系统通知分层；游玩中无线电保持非阻塞。
- 目标状态改用稳定编号，存档升至 v4 并兼容旧档；核心界面清除 Emoji 与调试版本标题。
- 顺手修复设置页完成时音量最终值未可靠同步和保存的问题。
- 自动验收：N0.2 8/8、核心 41/41、A0.1 6/6、A0.2 20/20、P1.1 15/15、P2 10/10 全部通过。
- Web Release 已更新到 `E:\项目\deep-mine\builds\web-live\index.html`；局域网与公网固定 `/play/` 均返回 200，浏览器实画面确认中文与井口对白正常，控制台错误 0。
- 严格未生成新 APK，最新 Android 正式测试包仍为 A0.3。
- 后续工作方式锁定：低风险执行默认由 Gemini 完成，Codex负责设计、拆解、风险决策与最终验收。
