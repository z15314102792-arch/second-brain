---
name: 2026-08-18-progress
description: 2026-08-18 进度——记忆丢失根因分析 + 方案 A+B 实施 + 第二大脑全量读取
tags: [日记]
metadata:
  type: reference
  modified: 2026-08-18
---

# 2026-08-18 进度

> 结束时间：2026-08-18 · 已保存进度

## 做了什么

### 1. 记忆丢失根因分析（完整回溯）
- **现象**：用户换硅基流动 API（deepseek-v4-pro → deepseek-v4-Flash）后，Claude Code 失忆。
- **过程**：用户坚持"之前换 API 没失忆"，要求深查。查 settings.json、查项目目录结构、读第二大脑后确认：
  - **根因不是换 API**，是**启动目录变了**：`C:\Users\Administrator\` → `C:\`
  - Claude Code 按启动目录分配项目身份：`C--Users-Administrator` → `C--`
  - 记忆是 per-project 的，新项目 `C--` 的 `memory/` 是空的
- **证据**：旧项目 `C:\Users\Administrator\.claude\projects\C--Users-Administrator\memory\` 有 5 个记忆文件，新项目 `C:\Users\Administrator\.claude\projects\C--\memory\` 空。

### 2. 第二大脑全量读取
- 按用户要求"全部读完，不要偷懒"，用 Glob + Read 读取全部 40+ 文件（知识/ 17 个、项目/ 14 个、资料/ 7 个、技能/ 6 个、日记 2 篇 + MEMORY.md + CLAUDE.md）
- 验证无遗漏（对比 MEMORY.md 索引逐项核对）

### 3. 方案 A+B 实施
- **方案 A**：旧项目记忆复制到当前项目 `C:\Users\Administrator\.claude\projects\C--\memory\`（5 个文件：user-profile.md、backup-to-second-brain.md、concise-reply.md、stuck-stop-loss.md、prefer-better-solution.md）✅
- **方案 B**：全局 CLAUDE.md 加「启动必读」章节，指定每次会话先读第二大脑 ✅
  - 文件：`C:\Users\Administrator\.claude\CLAUDE.md` → v2.1
  - 改动：在版本声明后新增"启动必读"小节（三行指令），排在"语言与沟通"之前

### 4. 讨论：是否需要额外钩子
- 用户否决了加 UserPromptSubmit 钩子的方案（"如果已经有机的话你强制执行不是浪费吗"）
- 结论：CLAUDE.md 指令已足够，无需钩子

### 5. 方案 B 优化（用户两轮反馈）
- **第 1 轮**：每次启动全读第二大脑太浪费 token → 改为只读入口文件（MEMORY.md + CLAUDE.md），有需要再读其他
  - CLAUDE.md v2.1 → v2.2
- **第 2 轮**：取消 G 盘推送（本机不连 G 盘）
  - 第二大脑 CLAUDE.md v4.3 → v4.4，删除 `git push hdd master`
  - remote 保留不删，以后连上还能用

### 6. 记忆文件同步
- 用户问 user-profile.md 是什么 → 确认第二大脑 `知识/用户人物画像.md`（v1.3）是主版本，Claude 项目 `memory/user-profile.md` 是快捷摘要
- 用户提醒：人物画像会变，有**时效性**
- 更新：两个文件版本同步，标注「上次更新：2026-08-18」
- 审计结论：**没有其他遗漏的记忆文件**，旧项目就 5 个

### 7. 根因补完
- 之前没有自动同步记忆的真正原因：**鸡生蛋问题**
  - 新项目空目录 → 读不到「同步记忆」规则 → 不知道要执行同步
  - CLAUDE.md v2.2 的「启动必读」指令已锁定这条链路，不再依赖项目记忆自举

## 待办
- [x] 方案 B 用户确认（已确认 + 优化两轮）
- [ ] **（下次）** 是否把「用户语言风格」加入项目记忆快捷入口
- [ ] 后续按规则：每次会话结束自动更新第二大脑 + git 同步

### 8. 口播自动剪辑工作流 v1.0 真实验收
- 按用户选择的第 1 种路径，保留无重编码分段裁剪与拼接版本。
- 修复 `transcribe.py`：改用 whisper.cpp 完整 JSON、解析中文时间戳、支持逐词 token、过滤特殊 token。
- 修复 `render_from_edl.py`：自动查找工作流内 FFmpeg/ffprobe。
- 使用真实素材完成转录、审核稿、EDL、重点候选和预览生成。
- 原始素材：64.066667 秒，720×1280，30fps，9:16。
- 识别统计：164 个 token，2 个候选，2 个 cuts，3 个 keep_ranges。
- 剪辑候选：11.730-12.310 长停顿 0.58 秒；25.370-25.640 语气词“啊”。
- 预览验收：59.150333 秒；视频流 59.122331 秒；音频流 59.150333 秒；720×1280；音视频流均存在。
- 抽帧验收：首帧、中段、末帧均有人物，无黑帧、无明显错位，竖屏构图正常。
- 曾尝试 30ms 音频淡入淡出，但导致输出时长错误变为 67.121029 秒，已撤销，不纳入当前方案。
- 当前待改：Non-monotonic DTS 兼容性、字幕、重点前置、B-roll/贴图、特效、剪映导出、近义重复识别增强。

### 9. DTS 安全渲染模式
- 在 `render_from_edl.py` 新增 `--mode copy|reencode`，`copy` 保持原默认行为，`reencode` 使用 FFmpeg concat 滤镜重新编码。
- 在 `run.ps1` 新增 `-RenderMode` 参数，默认 `copy`；安全调用：`-Render -RenderMode reencode`。
- 真实素材安全模式验收：59.120000 秒，视频 720×1280，视频流 59.100000 秒，音频流 59.120000 秒，DTS/错误警告数 0。
- 安全预览抽帧首帧、中段、末帧均有人物，竖屏构图正常，无黑帧或错位。
- 保留原无重编码预览 `preview.mp4`，安全输出另存为 `preview-safe.mp4`。

### 10. 字幕生成与烧录
- 新增 `build_subtitles.py`：把逐词转录按 EDL 映射到输出时间轴，生成可编辑 `master.srt` 和样式化 `master.ass`。
- 新增 `burn_subtitles.py`：在剪辑完成后最后烧录 ASS 字幕，输出 `final.mp4`。
- 真实素材生成 23 条字幕；字幕输出时间轴覆盖 0.01-59.11 秒。
- 字幕成片验收：59.120000 秒，视频 720×1280，视频流 59.100000 秒，音频流 59.120000 秒，中文字体加载成功。
- 抽帧验收：早期、中段、后段字幕均位于画面底部，白字黑边，未遮挡人物主体，无黑帧或画面错位。

### 11. 完整入口回归
- 在独立测试目录通过用户实际入口执行：`run.ps1 -Render -RenderMode reencode -BurnSubtitles`。
- 一次生成 `review.md`、`edl.json`、`master.srt`、`master.ass`、`preview.mp4`、`final.mp4`，23 条字幕。
- 入口输出验收：59.120000 秒，720×1280，视频流 59.100000 秒，音频流 59.120000 秒；分析验收字段全部为 true。
- 已把已验收的 `final-safe.mp4` 复制为标准交付文件 `final.mp4`，未覆盖已有文件。

### Claude Code 切换 DeepSeek 官方 API
- 将 `C:\Users\Administrator\.claude\settings.json` 切换到 DeepSeek 官方 Anthropic 兼容接口 `https://api.deepseek.com/anthropic`。
- 主模型、快速模型、子 Agent 模型统一为 `deepseek-v4-flash`。
- 实际请求验证成功；原配置备份为 `C:\Users\Administrator\.claude\settings.json.backup-2026-08-18-deepseek`。
- 用户粘贴的 API Key 已暴露，已提醒撤销并重新生成。

## 今日全部会话汇总（全量核对）

> 数据来源：`C:\Users\Administrator\.claude\projects\C--\` 下今日（2026-08-18）修改的全部 7 个会话文件，逐会话核对。密钥不在此明文记录。

| 会话 | 修改时间 | 主题 | 要点 |
|------|----------|------|------|
| s1 e89ae712 | 11:52 | AI 员工大会话 | 见下 |
| s2 7820a609 | 18:45 | 空测试 | 无实质任务 |
| s3 9429305d | 18:47 | 模型/API 选型 | 见下 |
| s4 12563138 | 18:50 | 空会话 | 无实质任务 |
| s5 625ac4ff | 19:35 | API 接入 | 见下 |
| s6 a1d946b9 | 19:42 | 失忆根因+图谱整理 | 见下 |
| s7 e7e513a8 | 19:43 | 本会话（查今日会话） | 本次任务 |

**s1（AI 员工）**
- 调研 AI 顶尖玩家实践 → 产出建议清单
- /goal 设"你进行第一个项目"；尝试记忆库项目（被批"不要偷懒"，CronCreate 被拒）→ 星月神殿（被叫停，用户已抛弃）→ 最终交付"全项目线上健康巡检"
- 五子棋四维 code review（安全/性能/规范/测试），修复 3 个问题
- 看图打通：智谱 GLM-4.6V 视觉脚本跑通；分析动物大战截图（鸡 vs 棕熊）可借鉴点
- 微信推送：Server酱 → 微信，用户已扫码拿到 SendKey；推送格式需升级（排版/解释/总结+分析）
- 清理 C 盘（用户自行解决）

**s3（模型选型）**
- 硅基流动 "Waiting for API response" 卡顿原因分析
- Claude 17 美元订阅额度缺口分析
- 要求 v4-flash vs v3.2 vs Qwen3.6-35B-A3B 全方面对比表（真实数据）→ 未产出
- 切火山引擎 deepseek-v4-flash 正式版 → 401 Token invalid，工具被安全分类器锁死，指导手动改 settings.json

**s5（API 接入）**
- 火山引擎接 deepseek-v4-flash 失败（小白操作，key 无效）
- 转 deepseek 官方 API → curl 测试 Authentication Fails / JSON 解析失败
- **结果已解决**：见上文第 12 节，已切换 DeepSeek 官方 Anthropic 兼容接口并验证成功

**s6（失忆根因 + 图谱整理）**
- 失忆根因、第二大脑全量读取、方案 A+B 已记录于上文第 1-7 节
- Obsidian 图谱整理：诊断出 11 个孤立「说明.md」节点 + 日报/配置链接稀疏；方案已定（说明.md 加 [[MEMORY]] 引用、各目录说明互链、日记互链），批量改未完成

### 未完成任务（已同步至 `E:\第二大脑\待办\2026-08-18-未完成.md`）
1. 微信推送最终落地（Server酱 已就绪，推送格式升级：排版/解释/总结+分析）
2. 模型全方面对比表（v4-flash / v3.2 / Qwen3.6-35B-A3B，真实数据）
3. 火山引擎 API 接入（401 待解决；已回退到 DeepSeek 官方，可选）
4. Obsidian 图谱批量整理
5. 「用户语言风格」加入项目记忆快捷入口（延续上文待办）
6. 口播工作流待改项（延续第 8 节：DTS 兼容、字幕、重点前置、B-roll、特效、剪映导出、近义重复识别）
