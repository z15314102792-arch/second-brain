# AI日报 | 2026-08-19

## 1. AI动态

### ★★ [AI usage patterns in software teams](https://linear.app/data)
- **发生了什么**：Linear 发布了一份关于软件团队中 AI 使用模式的数据报告，分析了开发者在实际工作中如何应用 AI 工具。
- **为什么重要**：这类基于真实使用数据的研究，能帮助开发者理解 AI 编程工具的落地场景和最佳实践，而非停留在概念层面。
- **跟我有啥关系**：作为个人开发者，可以参考报告中提到的团队协作模式，优化自己在小游戏项目中 AI 辅助开发的流程。

### ★★ [Show HN: Interactive, animated architecture of any HuggingFace models](https://modelmap.cc)
- **发生了什么**：一个名为 ModelMap 的工具，可以交互式、动画化地展示任意 HuggingFace 模型的架构。
- **为什么重要**：模型架构可视化降低了理解复杂 AI 模型的门槛，对学习和调试都有帮助。
- **跟我有啥关系**：如果你后续尝试在游戏中接入 AI 模型（如智能 NPC），这个工具能帮你快速理解模型结构。

---

## 2. 日志

### 昨天做了什么

- **解决 Claude Code 失忆问题**：完整回溯了记忆丢失的根因——不是换 API 导致，而是启动目录从 `C:\Users\Administrator\` 变成了 `C:\`，Claude Code 按目录分配项目身份，新项目的 `memory/` 是空的。
- **实施记忆恢复方案 A+B**：方案 A 把旧项目的 5 个记忆文件复制到新项目；方案 B 在全局 `CLAUDE.md` 增加「启动必读」章节，经过两轮优化（先改为只读入口文件省 token，再删除 G 盘推送）。
- **口播自动剪辑工作流 v1.0 真实验收**：用真实素材完成转录、审核稿、EDL、候选生成全流程；修复 `transcribe.py` 和 `render_from_edl.py`；新增 DTS 安全渲染模式（`--mode reencode`）、字幕生成与烧录（`build_subtitles.py` + `burn_subtitles.py`），最终通过完整入口 `run.ps1 -Render -RenderMode reencode -BurnSubtitles` 一次跑通。
- **切换 DeepSeek 官方 API**：将 `settings.json` 切换到 DeepSeek 官方 Anthropic 兼容接口，主模型、快速模型、子 Agent 统一为 `deepseek-v4-flash`，实际请求验证成功。
- **全量核对今日 7 个会话**：逐一提取主题与完成状态，确认 s5 的 DeepSeek API 已成功配置；整理出 6 个 API key 入库（密钥文件已 .gitignore，不上云）。
- **项目运行状态**：中国象棋、五子棋、你画我猜、动物大战、螺丝消除均正常，星月神殿 404 页面缺失。

### 遇到的问题和卡点

- **记忆丢失根因定位**：用户坚持"之前换 API 没失忆"，排查一度陷入僵局，后来通过对比新旧项目目录结构才找到真正原因。
- **口播剪辑 DTS 错误**：30ms 音频淡入淡出导致输出时长错误（67.12 秒 vs 59.15 秒），已撤销该方案。
- **火山引擎 API 接入失败**：401 Token invalid，工具被安全分类器锁死，只能手动改 `settings.json`，最终回退到 DeepSeek 官方 API。
- **API Key 明文暴露**：用户粘贴的 API Key 出现在会话中，已提醒撤销并重新生成；手机端 DeepSeek key 也明文出现过，已在密钥库备注。

### 怎么突破的

- **记忆丢失**：不轻信表面现象，深入对比新旧项目 `memory/` 目录内容，用证据说话——旧项目 5 个记忆文件 vs 新项目空目录。
- **DTS 问题**：新增 `--mode reencode` 安全渲染模式，用 FFmpeg concat 滤镜重新编码，真实素材验证 DTS/错误警告数为 0。
- **API 接入**：火山引擎失败后迅速切换 DeepSeek 官方 Anthropic 兼容接口，curl 测试通过后完成配置，原配置备份为 `.backup` 文件。

### 遗留待办

- [ ] 微信推送最终落地（Server酱 已就绪，推送格式升级：排版/解释/总结+分析）
- [ ] 模型全方面对比表（v4-flash / v3.2 / Qwen3.6-35B-A3B，真实数据）
- [ ] 火山引擎 API 接入（401 待解决；已回退到 DeepSeek 官方，可选）
- [ ] Obsidian 图谱批量整理（11 个孤立「说明.md」节点）
- [ ] 「用户语言风格」加入项目记忆快捷入口
- [ ] 口播工作流待改项（DTS 兼容、字幕、重点前置、B-roll、特效、剪映导出、近义重复识别增强）
- [ ] 每次会话结束自动更新第二大脑 + git 同步

### 可以改进的地方

- **环境变更前先确认目录**：这次失忆的根因是启动目录变了。以后更换 API、迁移环境时，先确认工作目录是否一致，避免重蹈覆辙。
- **密钥管理要前置**：API Key 明文出现在会话里已经不是第一次。建议在 `CLAUDE.md` 里加一条硬性规则——涉及密钥的操作必须先提醒用户打码，从源头杜绝暴露。
- **新功能先做最小验证再集成**：30ms 音频淡入淡出导致时长错误，说明小改动也可能引发大问题。建议在 `run.ps1` 里加一个 `-DryRun` 参数，先跑一遍元数据检查再实际渲染。
- **「鸡生蛋」问题的通用解法**：新环境读不到旧规则导致不知道要同步——这个教训可以抽象成通用原则：任何"自举"型规则都要有一个外部入口（如全局 CLAUDE.md）做兜底。